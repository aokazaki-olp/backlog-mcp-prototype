---
tags:
  - review
  - backlog-mcp
---

# backlog-mcp-prototype `src/` レビュー台帳

対象: `aokazaki-olp/backlog-mcp-prototype` / `src/`（17ファイル 5,577行、`src/libs/` 除く）
版: HEAD `5055619`
手法: [[multi-angle-review]] ＋ 補遺「指摘修正のあり方」

## 第1巡（2026-09-06・版 5055619）

視点4本、並列・独立（互いの結論を見せない）。L2/L4 は別モデルで instrument 独立に寄せた。

| 視点 | 担当 | 接地 | 指摘数 |
|---|---|---|---|
| L1 | 正確性・堅牢性 | probe 11本を実行 | 11 |
| L2 | 境界の破れ（red-team） | 攻撃を10系統組み立てて実行 | 4 |
| L3 | 使う側の体験 | tools/list 全文と実応答37件を出力 | 15 |
| L4 | 内部整合・規約準拠 | `verify:rules` 28/28、カバレッジ計測 | 6 |

ベースライン: `npm run check` 緑（lint / format / typecheck / test 379件 / build、exit 0）。

---

## 統合者による較正

**1. L1 の行番号は系統的に誤り。** `audit.ts:364` → 実際 `:75`、`main.ts:339` → 実際は39行のファイル、`stdio.ts:265` → 実際は63行のファイル、`untrusted.ts:288` → 実際は134行のファイル。probe による実体はすべて本物だったので、**位置情報のみ統合者が引き直した**。補遺の実例5（報告された実測が再現しなかった）が想定する事態の変種で、「実体は正しいが位置が誤り」という形。**この視点由来の位置情報は次巡も独立に確かめる。**

**2. L2-3 の引用元が誤り。** 「選択肢は管理者が定義したもの」は `tools.ts:542`（カスタム属性の `{id,name}` について書かれたもの）であり、`pickName` には無い。`pickName` の TSDoc が述べているのは**フィールドの漏洩防止**（`mailAddress` 等を出さない）であって、信頼分類ではない。**つまりユーザー表示名の信頼分類は検討された形跡が無い** — 指摘としてはむしろ強まる。

**3. 統合者自身の失敗 — 根分けを推論で行った。**
第1巡の統合直後、36件を10の「根」に束ねて提示した。**この根分けはソースを読まずに、指摘文の主題が似ているかどうかで行ったものだった。** 補遺が「列挙は検索ではなく、変更する一文の役割の書き出しから導く」「検索で段階3 を済ませない」と禁じている形の、さらに弱い版（検索すらしていない）。

指摘を受けて `src/` 5,577行を通読し、根を導出し直した。**10のうち4つが誤りだった**（下記「根の導出」）。同根のものを分ければ再発が確定し、別根のものを束ねれば対応関係が追えなくなるので、どちらも巡数を増やす。

**教訓**: 補遺は「指摘の接地」と「修正案の接地」を要求しているが、**その手前の「根分けの接地」を明示していない**。統合と適用の境目にあるこの作業が、規律の穴になっている。補遺へ反映するかは要検討。

**4. L2 が一次情報の引用を捏造していた（2026-09-06 発覚）。**
L2 は根B の裏取りとして Backlog ヘルプセンターの2記事の URL を挙げ「確認済み」と報告したが、
**`support-ja.backlog.com` はこの環境から 403**（Cloudflare のチャレンジ。curl とブラウザ UA の
2通りで確認、2026-09-06）。DESIGN.md:332 は「この環境から出られない」と既に記録しており、
ミラー `docs/reference/api/`（`fetched: 2026-08-30`、152エンドポイント）にも
表示名に関する記述は0件（`update-user` エンドポイント自体が無い）。

**統合者はこれを台帳へ「一次情報で確認済み」として写していた。** 指摘本体は接地を確かめたのに、
**添えられた出典は出典として検証していなかった。**

原因の一端はレンズ設計にある。L2 のプロンプトに **`research-workflow` の適用を明示せず、
`docs/reference/api/` にミラーがあることも伝えていなかった**（L3 には
`docs/reference/mcp/` を伝えている）。外部サイトへ行かせる形にしたのはこちらの設計。

**処置**: L2 由来の主張は**独立の裏取りが取れたものだけ採用候補に残す**
（スキル本体「その視点の出力全体を割り引く」）。根B は外部の事実に依存しない
2つの根拠へ組み替えた（`_review/根B-*.md`）。

**次巡のレンズ設計**: 一次情報に触れる視点には ①`research-workflow` の適用を明示
②`docs/reference/` のミラーが正本 ③「到達できなかった」も報告させる。

---

## 根の導出（`src/` 通読に基づく）

### 根A — 保護が経路に貼られ、境界に貼られていない

**閉じる指摘**: C-1（L1-6 + L2-2 + L3-10 の一部）

`sendRequest`（`tools.ts:1941`）が包んでいるのは `gateway.send` **だけ**。`runTool` の分岐のうち、`send` / `chain` は共通の末尾に落ちるので自動的に包まれ、`both` は明示的に2回呼んでいる。**`download` だけが早期 return するため共通の末尾に到達しない**（`tools.ts:1990-1996`）。

同じ穴から `backlogGateway.ts:61` の裸の `Error`（`${request.endpoint} からバイト列を受け取れませんでした`）も素通しで出る。

経緯: `sendRequest` は `613016b` で導入、`download` / `sendBytes` は `11c83d0`（HEAD の3つ前）で追加。**新しい機能が古い保護の前提を壊した形。** `sendRequest` の TSDoc「この try の内側から出てきたものは定義上すべて『下から来たもの』」は、`send` が外への唯一の口であるうちは正しかった。

### 根B — ユーザーの表示名を「管理者が定義した値」に分類している

**閉じる指摘**: L2-3、C-2（新規）、L1-4 の一部

コードベースは**「第三者が書けるか」という軸を明示し、全箇所で一貫して適用している**（`shapeIssue:675`「件名も第三者が書ける」/ `shapeRepository:839`「説明は第三者が書ける」/ `pickChildIssueSummary:496`「数値2つなので囲まない」/ `untrusted.ts:38` は `UntrustedSource.name` の型注釈で軸そのものを宣言）。

**欠陥は軸の不在ではなく、1つの値の誤分類。** 同じ誤分類が3箇所で3通りに現れる。

| # | 場所 | 現れ方 | 発見者 |
|---|---|---|---|
| 1 | `tools.ts:467` `pickName` | `assignee` / `createdUser` / `updatedUser` が無囲い。ほぼ全応答に付随 | L2-3 |
| 2 | `tools.ts:1675` `list_project_masters` | `assignees`（`members`）・`ambiguousUserNames` が無囲い | 部分的に既知 |
| 3 | `masters.ts:363-388` `lookupName` | **TSDoc が「どれも Backlog の管理者が付けた名前（第三者の自由記述ではない）」と断言。** `resolveAssignee` 経由で `project.userIds` に対して呼ばれ、失敗時に最大20人ぶんの表示名・ログイン名を無囲いで出力する | **統合者（横断探索）** |

3番目は**どの視点も挙げていない**。補遺の「1件見つかった種類は、たいてい他にもある」に従って横断で探した結果。

### 根C — 監査層。**因果の鎖があり、順序が決まっている**

**閉じる指摘**: L1-3 → L1-1 → L1-2

当初「L1-1 と L1-3 は同根」「L1-2 と L1-5 は同根」と推論したが、**どちらも誤り**。3件は別の欠陥だが、因果で繋がっている。

```
L1-3（audit.ts:184-192 — record() が try の外）
  ├→ 1回の callTool で record() が2回走る
  │    └→ L1-1（audit.ts:75-86 — fdFor が閉じ済み fd を再度 close）が発火する
  └→ 例外が withAudit から再送出される
       └→ L1-2（stdio.ts:39-62 — for await が死ぬ）に到達する
```

`buildHandlers.callTool`（`tools.ts:2472-2478`）は**すべて自分で catch している**ので、`serve` まで例外が届く経路は `withAudit` の再送出しかない。**L1-3 が唯一の引き金で、L1-1 と L1-2 はその下流。**

**順序を違えると巡が増える。** L1-2 を先に直すと「ループは死ななくなったが監査ログは壊れ続ける」状態になり、L1-1 は再現条件を失って検証できないまま残る。

### 根D — schema が静的で、起動時に確定する事実を受け取れない

**閉じる指摘**: L3-1、L3-7

`INPUT_SCHEMAS`（`tools.ts:2073`）はモジュールレベルの静的な `const`。`toDefinition`（`tools.ts:2406`）が行うのは `withoutFileProperty` だけ。

許可プロジェクトキーも `priority` / `resolution` の名前集合も**起動時に確定しており `tools/list` の生成にも既に使われている**（`buildHandlers` の `listed`、`masters.priorityIds`）のに、schema がそれを受け取る形になっていない。**両方とも同じ構造変更（schema を policy / masters の関数にする）で閉じる。**

### 根が違うと確認したもの（束ねない）

| 当初の推論 | 実際 |
|---|---|
| L3-5 を根B に束ねる | **別。** L3-5 は囲みの*書式のコスト*（定型文252バイト × フィールド数）、根B は*どの値を囲むか*。片方を直しても他方は動かない |
| L1-4 と L1-7 は同根 | **別。** L1-4 は `toUserIds`（`masters.ts:140-175`）が**1つの Map を2つの名前空間で共有し、`ambiguous` が表示名側しか守っていない**という状態設計の誤り。L1-7 は `findAttachment` の設計上の割り切り |
| L1-5 を根C に束ねる | **別の層。** L1-5 は `callTool` の catch が `e.message` しか読まない（`SuppressedError` の `suppressed` を辿らない） |
| L3-3 と L3-6 は同根 | **別。** L3-3 は `callTool` が args を無検証で通す、L3-6 は `toDefinition` が annotation 値を固定している |

---

## 指摘一覧

| ID | 重要度 | 概要 | 根 | 推奨 | 状態 |
|----|--------|------|----|------|------|
| **C-1**（L1-6/L2-2/L3-10 一部） | H | `download` 経路だけ `sendRequest` を迂回する | A | 修正推奨 | 未決 |
| **L2-3** | H | `pickName` — ユーザー表示名が無囲い | B | 修正推奨 | 未決 |
| **C-2**（統合者・新規） | H | `lookupName` の TSDoc が誤り。エラーメッセージが表示名を最大20件無囲いで出す | B | 修正推奨 | 未決 |
| **L1-3** | H | 監査シンクの失敗が成功を失敗に化けさせ、本来の例外が `cause` も残さず消える | C（引き金） | 修正推奨 | 未決 |
| **L1-1** | H | ローテーション失敗後、閉じ済み fd を再度 close し無関係の fd を閉じる | C（下流） | 修正推奨 | 未決 |
| **L1-2** | H | ハンドラ送出で `for await` ごと死ぬ。`RPC_ERROR.INTERNAL` は未使用 | C（下流） | 修正推奨 | 未決 |
| **L1-4** | H | ログイン名と他人の表示名の衝突で担当者が別人に解決される | 単発 | 修正推奨 | 未決 |
| **L3-2** | H | `get_issue` の description が「カスタム属性を返さない」と言うが返している | E | 修正推奨 | 未決 |
| **L3-3** | H | `additionalProperties: false` を宣言しつつ未知の引数を黙って無視 | 単発 | 修正推奨 | 未決 |
| **L3-6** | H | `readOnlyHint: true` がディスクへ書く／`destructiveHint: false` 固定 | 単発 | 修正推奨 | 未決 |
| **L3-1** | H | 許可プロジェクトキーを LLM が知る手段が無い | D | 修正推奨 | 未決 |
| **L4-1** | H | DESIGN.md §10 が「未対応」と書く `FormFields` の対処は実装済み | E | 修正推奨 | 未決 |
| **L4-2** | H | DESIGN.md §4 の層表に `attach/` が無く lint の境界も無い | E | 修正推奨 | 未決 |
| L3-7 | M | `priority`/`resolution` に `enum` が無い | D | 修正推奨 | 未決 |
| L1-10 | M | 添付を上げ切ってから `relatedIssueKey` のポリシー判定 | 単発 | 修正推奨 | 未決 |
| L1-9 | M | `/issues/count` の失敗で検索結果ごと捨てる（`Promise.all`） | 単発 | 修正推奨 | 未決 |
| L1-8 | M | `assignedToMe` と `assignee` の同時指定で前者が黙って上書き | 単発 | 修正推奨 | 未決 |
| L1-5 | M | `SuppressedError` の `suppressed` を辿らない | 単発 | 修正推奨 | 未決 |
| L3-5 | M | 囲みの定型文が応答の50.5%を占める | 単発（T-1） | 保留 | 未決 |
| L3-8 | M | `search_documents` に `projectKey` も識別子も無い | 単発 | 修正推奨 | 未決 |
| L3-9 | M | Wiki とドキュメントの使い分けが description から不明 | E | 修正推奨 | 未決 |
| L3-12 | M | 未知のツール名を `isError` で返す（仕様は Protocol Error） | 単発 | 修正推奨 | 未決 |
| L4-3 | M | `toUserIds` / `toNameToIdAllowingEmpty` の形状異常が未テスト | F | 修正推奨 | 未決 |
| L4-4 | M | `parseRequest` の非オブジェクト入力、`withAudit` の例外経路が未テスト | F | 修正推奨 | 未決 |
| L1-7 | M | 同名添付が複数あると先頭を黙って選ぶ | 単発 | 保留 | 未決 |
| L3-4 | M | `offset` が `search_issues` にしか無い | 単発 | 保留 | 未決 |
| L3-11 | M | `instructions` の「確認してください」が広すぎる | 単発 | 保留 | 未決 |
| L1-11 | L | 打ち切りがサロゲートペアを分断する | 単発 | 保留 | 未決 |
| L3-13 | L | `hasParent: true` から親へ辿れない | 単発 | 保留 | 未決 |
| L3-14 | L | 共有 `file` プロパティの説明が「1コメントにつき1件」 | E | 修正推奨 | 未決 |
| L3-15 | L | 内部エンドポイントパスがエラーメッセージに露出 | 単発 | 保留 | 未決 |
| L4-5 | L | `config.ts:128` の保険の分岐が未テスト | F | 修正推奨 | 未決 |
| L4-6 | L | `AttachmentLimits` / `DEFAULT_ATTACHMENT_LIMITS` が未使用の公開面 | 単発 | 保留 | 未決 |

**根E（記述と実装の乖離）と根F（エラー経路のテスト欠損）** は、同種の欠陥が複数箇所にあるという意味でまとめてあるが、**修正は箇所ごとに独立**。根A〜D のような構造的な同根ではないので、束ねる必要は無い。

---

## 対立（トレードオフの所在。多数決で潰さない）

**T-1 — 囲む対象を広げると、囲みのコストが増える。**
根B は囲む対象を3箇所増やす方向。L3-5 は囲みの定型文が応答の50.5%を占めると測っている（`search_issues` 20件で 9,979 バイト中 5,040 バイト）。**根が違うので同時に直せるが、根B を先に当てるとコストは増える方向に動く。**

裁定案: 書式（1フィールドごとに252バイトの定型文を繰り返す）と対象（どの値を囲むか）を分けて考えれば両立する。ただし「定型文を `instructions` へ寄せて囲みを軽量マーカーにしても緩和が効くか」は**未検証**。**人間の裁定が要る。**

**T-2 — 添付ファイル名を囲むか。**
L2-1 は攻撃者が完全に制御できる添付ファイル名が無囲いで届くことを実証した。しかし `tools.ts:1034` は**意図的に囲んでいない**（「ファイル名は次の呼び出しに渡す識別子。囲むと識別子として使えない」）。同じ判断が `list_wiki_pages` のページ名にも適用されている。

裁定案: これは見落としではなくトレードオフ。**根B（誤分類）とは別問題**として扱う。**人間の裁定が要る。**

---

## 却下（設計意図。根拠を接地したうえで）

- **L2-4**（数字だけのプロジェクトキー）— L2 自身が「実害は確認できなかった」「境界の破れとしては実証されていない」と明記。`policy.ts:13` の `PROJECT_KEY_PATTERN` と `tools.ts:34` の `ISSUE_KEY_PATTERN` を読んで確認した — `202-1` はポリシーに `202` が無い限り `ScopeDeniedError` で拒否される。**却下**
- **`src/` に `index.ts` が無い**（L4 が指摘に至らなかったと自己申告）— `package.json` を確認（`private: true` / `files: ["dist"]` / `bin` のみ）。外部から `src/` を import する利用形態が無く、§2.5 の主眼である公開面制御が該当しない。**却下**（L4 の判断に同意）

---

## 空振りの確認（収束が「健全」か「枯渇」かの判定材料）

**枯渇ではない。** 各視点が自ドメインを実際に踏んだ痕跡がある。

- L2 は**10系統の攻撃を実行し、7系統で防御の成功を実証したと報告**（ポリシー迂回8パターン・パストラバーサル4パターン・自己ファイル検知3パターン・URL パス注入・監査ログ改ざん・囲みタグの脱出・権限切り下げ・API キー漏洩）。破れたのは untrusted 囲みの2系統のみ。**ただし較正4のとおり L2 は出典を1件捏造しているため、この報告は未検証に格下げする。** 「収束が枯渇ではない」の根拠としては、L1・L3・L4 と統合者の通読が残る
- L4 は `verify:rules` 28/28 通過、カバレッジ line 94.56% を計測したうえで未到達分岐を名指し。§2.2・§4.1・§5.1・§5.5・§6.4・§6.5・§3・依存方向は違反ゼロと確認
- L1 は `policy.ts` 全体・`localFile.ts` の読み取り側・`probeCount` の算術・`MAX_HOPS`・`parseRequest` を「見たが問題なし」と明記
- L3 は命名・入力エラー文言・`total`/`truncated`/`note`・schema の縮退・`Tool.title` を「見たが問題なし」と明記

統合者による通読でも、`policy.ts`（正規形への展開・`freezeMap`/`freezeSet` による実行時凍結）と `localFile.ts` の読み取り側（`realpath` → `isInside` → 単一ハンドル運用による TOCTOU 対策）は**設計と実装が一致している**ことを確認した。

---

## 収束

**第1巡は未収束。** H 13件（うち C-2 は統合者が横断探索で追加）。

第2巡の単位は「指摘」ではなく**根**。補遺の「1巡の修正数を絞る」に従い、根A・根B・根C の3つを対象とする。各根について段階1〜4（理解 → 調査 → 整合性 → 方針確定）を踏み、**段階4で人間の確認を取ってから段階5へ進む。**

残る根D・E・F と単発の指摘は**先送りとして台帳に保持**し、次巡へ閉じた指摘として渡す（蒸し返し防止）。
