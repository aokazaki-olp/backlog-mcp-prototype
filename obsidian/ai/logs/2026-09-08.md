---
date: 2026-09-08
type: log
---

## 00:03 — 第1巡のレンズが文脈保持で復帰することを実測し、起動手順を文書化した

### 事実（前提が2回覆った）

「レビューを回したサブエージェントは別セッションで使えるか」を聞かれ、
`ListAgents` に出ないことを根拠に「使えない」「プロンプトもどこにも無い」と答えた。**両方誤り。**

| 主張 | 実際 |
| --- | --- |
| インスタンスは消えた | **`SendMessage` で復帰する。**「名前は完了後も有効で、送信するとトランスクリプトから復帰する」がツールの仕様 |
| プロンプトはどこにも無い | **セッション記録に残っていた**（`~/.claude/projects/…/86687848….jsonl`、3.6MB）。4本とも回収 |

`ListAgents` は「いま一覧できるか」に答える道具で、「到達できるか」には答えない。
**道具が答えていない問いの答えとして読んだ、この一連の作業で4回目の誤り。**

### 事実（文脈保持の実測）

「ファイルを一切読まずに記憶だけで答えよ」と4本へ送った。**全て `tool_uses=0` で正確。**

| 視点 | 保持していた文脈 | 記憶から答えた内容 |
| --- | --- | --- |
| L1 | 196,968 tokens | `fdFor` の二重 close、probe 名（p1 / p11）、`marker.txt` まで |
| L2 | 234,214 tokens | 破れた2系統 |
| L3 | 164,923 tokens | **252 / 9,979 / 5,040 バイト / 50.5%**（数値まで一致） |
| L4 | 253,702 tokens | L4-2 と「指摘に至らなかった観察」 |

### 事実（作ったもの）

`_review/lenses/` に5本。README（起動手順）＋ 視点4本のプロンプト。

- **経路1（文脈保持）** — `SendMessage` に**エージェント ID** を渡す。
  **ID はこの文書にしか無い**（第1巡の会話のツール結果にしか現れず、台帳にも書いていなかった）
- **経路2（縮退）** — 別マシン用。`L*.md` を丸ごと新規エージェントに渡す

第1巡のプロンプトの欠陥（L2 に `research-workflow` とミラーを伝えていなかった＝出典捏造の一因）を
**4本すべてで修正**。L1 には行番号の較正も足した。

### 解釈

**縮退手順を先に作ってしまった。** ユーザーの「このセッションなら文脈保持で起動可能?」で
気づいて組み替えた。**確かめてから設計する順序を、また逆にしていた。**

`/tmp` の `tasks/*.output` は既に消えていた。**L2 の攻撃 probe の中身と L3 の `tools/list` 全文は、
報告として書き写したぶんしか残っていない** — OS が掃除する場所に置かれるので、
残したいものは報告に書き出すしかない。

## 00:15 — 未対応20件を全件修正した（L1 6件・L3 6件・L4 1件＋ID無し2件）

### 事実

**人間の裁定「全部修正する」**を受け、保留7件を含む残り全件を当てた。着手前に
**`src/` 17ファイル・約6,100行を通読**し、各件の接地と seam と波及を先に出した（段階2〜3）。

裁定が要った点は4つ。①同名添付は**先頭を返して件数を載せる** ②親の課題キーは
**`get_issue` だけ1手足す** ③全員の名前が曖昧でも**起動を止めない** ④エンドポイントパスは
**`asArray` の12箇所も日本語の語彙へ**。

| 件 | 直したもの | 既存テスト |
| --- | --- | --- |
| L1-5 | `SuppressedError` の向き。`shared/suppressed.ts` を足して `callTool` と `main.ts` から使う | 動かず |
| L1-11 | 打ち切りがサロゲートペアを割る。`shared/cutCodeUnits.ts` | 動かず |
| L1-8 | `assignedToMe` × `assignee` の黙った上書き | 動かず |
| L1-9 | 件数の失敗で検索結果ごと捨てる。`both` の2本目を**補助**として型に出した | **1件** |
| L1-10 | 添付を上げ切ってからポリシー判定。判定と ID 解決を割った | 動かず |
| L1-7 | 同名添付の黙った選択 | 動かず |
| L3-15 | エンドポイントパスの露出（`asArray` 12箇所＋`backlogGateway`） | **1件** |
| L3-12 | 未知のツール名を Protocol Error へ。`UnknownToolError` を `contract.ts` に | **2件** |
| L3-4 | `offset` を API が持つ3本のうち残り2本にも開けた | **2件** |
| L3-8 | ドキュメントに `projectKey`（絞り込みと出力の両方） | 動かず |
| L3-13 | `get_issue` が親の課題キーを返す | **ヘルパー1箇所** |
| L3-9 / L3-14 / L3-11 | 記述（Wiki とドキュメントの使い分け／共有 `file`／`instructions` の範囲） | 動かず |
| L4-6 | `DEFAULT_ATTACHMENT_LIMITS` の export を落とす | 動かず |
| ID無し① | `toUserIds` が空でない応答に「応答が空です」 | 動かず |
| ID無し② | `shared/` と `mcp/` の lint 境界 | — |

ゲート: `npm run check` 緑（**503件** pass。436 → +67）／`verify:rules` **32/32**（29 → +3）。

### 解釈

**「使う側から見て不揃い」の半分は、API の側の事情だった。** L3-4 をミラーで裏取りしたら、
`offset` を持つのは152本のうち**3本だけ**で、`search_issues` はその1本だった。
「他にも付けろ」ではなく「**持つところは開け、持たないところは description に書け**」が答えになった。
**指摘の言葉をそのまま設計の指示として読むと外す。**

**L3-13 で既存テストが27件動いたが、期待値は1つも変わっていなかった。** 全部
テストヘルパーの1行（「`get_issue` は1往復で終わるはず」）で、**ヘルパーが旧仕様を持っていた**。
手順5 の「既存テストが動くなら seam を疑う」は正しく発火したが、疑った先は seam ではなく
**ヘルパーに固定された仕様**だった。**この型を見分ける語彙が要る。**

**`git stash` で「外すと落ちるか」を確かめようとして失敗した。** 自作のテストごと消えるので
`fail 0` になり、確認になっていない。**実装だけを戻す**のが正しい。

**裏取りが「記述が無い」で終わることがある。** L1-7 の「同名はリネームされる」は
API ミラー152本とヘルプセンター ja 110記事を全走査して**記述ゼロ**だった。
**どちらの仮説でも正しい直し方**（先頭＋件数）を選べたので、確定させずに進めた。

### 決定

- **保留7件は却下せず、全件採用した**（人間の裁定）。台帳の状態列は触っていない
- **ID無し2件も直した**。①は `根F-*.md` が「設計の判断」として保留していた論点で、そこに決着を追記
- **`mcp` 層は `tool` からの import も禁じた**（依存は `tool → mcp` の一方向。規約 §2.6）
- **L1-7 は「未確認」とコードと README に明記**。実スペースで確かめる項目として残す
