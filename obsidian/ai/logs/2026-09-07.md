---
date: 2026-09-07
type: log
---

## 07:01 — 監査ログに既製品を使えるかを調べ、実機で確かめた

### 事実（きっかけ）

「これだから既存のパッケージを使いたかった」という一言から。**ロギングフレームワークではなく、
この監査ログに最適化された部品があるか**を調べた。結果を `_review/02-監査ログに既製品を使えるか.md` に置いた。

### 事実（npm の実態）

`registry.npmjs.org/-/v1/search` で「audit log」「audit trail」「tamper-evident log」等を引くと2群に割れる。

- **フレームワークのプラグイン**（Loopback / Payload / Strapi / Backstage / AdminForth）— ORM や CMS の
  「誰がどのレコードを変えたか」で用途が別
- **2026年産の AI エージェント監査** — `mcp-audit-log`（2リリース・3日）・`@agentlaw/audit`（1）・
  `tamper-evident-log`（2）・`logroller`（5リリース・2日）。**全部が生まれたて**

`mcp-audit-log` は名前も機能一覧も当たっており、README が
「**If audit logging fails, the MCP server continues operating normally**」と、
根C で未決の設計判断そのものに答えている。ただし2026-03-26 で止まり、`repository` も無い。

### 事実（前提が覆った — 探す層が高すぎた）

**`sonic-boom`（pino の `destination` そのもの）が条件をほぼ満たす。**
5.0.1 / MIT / **65リリース・9年** / **依存1**（`atomic-sleep`）。
`sync` / `fsync` / `mode` / `mkdir` / **`reopen([file])`** を持ち、**レベルも worker も無い**。

**DESIGN.md §5 の反論はこれに掛からない。** §5 が測っていたのは pino / winston / bunyan という層で、
**その下の層を見ていなかった。**

### 事実（実機・Node v24.20.0・`sonic-boom@5.0.1`）

スクラッチパッドに別プロジェクトを作り、2パッケージだけ入れて動かした。**本体は触っていない。**

| 確かめたこと | 結果 |
| --- | --- |
| ESM から CJS を読めるか | `import SonicBoom from 'sonic-boom'` は動く。`import { SonicBoom }` は `undefined` |
| `sync: true` の書き抜け | 無い（`minLength` 既定 `0`） |
| `mode` / `mkdir` / `fsync` | すべて効く |
| **失敗の伝わり方** | **listener あり→イベント／なし→同期送出**。`reopen()` の失敗だけは両方 |
| **L1-1 の等価シナリオ** | **sonic-boom は正しい。** 開けなかったとき古い fd を閉じず、二重 close が起きない |

理由はソースにあった（`index.js:527-538`）。**「開けてから閉じる」**。
今の `fdFor`（`audit.ts:75-86`）は逆で「閉じてから開く」。

### 事実（L1 の報告より1段悪い形が出た）

現行ロジックを写して再現したところ、**`EBADF` は起きなかった。**
閉じた番号を次の `openSync` が即座に取り直すので、**犠牲になった側の `write` は成功したまま、
中身が監査ログのファイルへ入る。**

```
現行（fd を戻さない）   marker の書き込み先: **別ファイルに入った**
提案（fd = -1 を足す）  marker の書き込み先: OK（自分のファイルに入った）
```

**エラーが出ないぶん重い**（規約 §5.4）。**再現テストは「書いた内容がどのファイルに入るか」で
固定しないと素通りする** — 「fd が生きているか」だけを見ると、番号が再利用されるので生きて見える。

### 事実（「レベルを ALL で固定する」案の検討）

壁打ちで出た案。**成立する。私の反論2つが崩れた。** pino には `sync: true` があり、レベルも固定できる。
崩れなかったのは1点で、`docs/api.md` 1103行目が **`logger.level` (String) [Getter/Setter]**。
`logger.level = 'silent'` の1行で消え、`silent` は **noop** なので投げも警告もしない。

### 解釈

**L1-1 は既製品の守備範囲だが、1行で塞がる。** そして採用すると根C の裁定が
**EventEmitter の作法で決まってしまう**（listener を付けるか付けないかが「続ける／伝える」になる）。
これは §5 が「監査がレベル体系に載る」を嫌ったのと同じ形なので、**裁定が先**。

**DESIGN.md §5 は書き直す価値がある。** 「非同期だから」「レベルで消えるから」を根拠にしていると
**設定で答えられて理由が崩れる**（実際2つとも崩れた）。書くべきは**「監査は消せる口を持たない」**で、
これは根A の修正（生の gateway を持たせない）と同じ原理。**同じ原理が2箇所で効いている。**

### 事実（このファイルについて）

**JST で日付が変わったので `2026-09-07.md` を作った。** `2026-09-06.md` の 06:32 以降の3エントリは
日付をまたいだ後に 09-06 側へ入っているが、**過去エントリは不可侵**なので触らない（[[obsidian-log]] の
「1日1ファイル」に戻すのは本エントリから）。

---

## 08:09 — 根C を段階3〜5 まで通し、監査層を修正

### 事実

**裁定を人間から取った（2件）。**

1. **稼働中に監査が書けなくなったら止める。** ただし「意図した停止」にする
2. **直前の1件には結果をそのまま返し、同じ応答に利用者への告知を1ブロック足す。**
   `isError` には載せない（仕様が `isError` を「モデルが自己修正して**再試行**するための feedback」と
   定義しており、再試行こそ最悪の結果 — Backlog への投稿は済んでいるので二重投稿になる）

裏取りは `docs/reference/mcp/specification/2026-07-28/server/tools.md` §Error Handling。
Server errors はプロトコルエラー側に名指しされ、tool execution error は
「クライアントは LLM に見せる**べき**（SHOULD）」、protocol error は「見せて**よい**（MAY）」。

**実測を3件取った（すべて自分で走らせた。Node v24.18.0）。**

- **L1-1 の再現**: 実装者ノートと同じ結果（`fd=20`, `markerFd=20`）。**`EBADF` は起きず、
  巻き添えの書き込みが監査ログのファイルへ入る**。L1 の報告より1段悪い
- **sonic-boom 5.0.1 の失敗経路**: 構築も `reopen` も**同期送出**。失敗後は古い fd を保持したまま
  書き続ける（`"day06\nafter\n"`）。`uncaughtException` はこの経路では出ない
- **型と実行時が正反対**: `import SonicBoom from 'sonic-boom'` は実行時に動くが型で `new` できない
  （TS2709 / TS2351）。`import { SonicBoom }` は型が通るが実行時に `SyntaxError`。
  両方を満たすのは `import SonicBoomModule` ＋ `import type { SonicBoom }` ＋
  `new SonicBoomModule.SonicBoom(...)` の形だけ（`as` 不要・lint も通る）

**sonic-boom のソースに同種の欠陥を見つけた**（`index.js:527-531`）。`reopen` は `once('ready')` を
**開く前に**登録するので、開けなかったときリスナが残り、次に成功したとき**同じ fd を2回 close する**。
ただし決定的ではなく、失敗時にデータを失わず、`'error'` に `EBADF` が流れる（現行の手書きは
決定的・沈黙・気づけない）。**裁定「止める」を入れると sonic-boom 側の経路は到達不能になる。**

**修正**（`src/` 5ファイル・`tests/` 2ファイル）。

- `AuditSink` の契約に「`write` は投げる」を書いた（型は変えていない）
- `AuditUnwritableError`（`result` と `suppressed` を持つ）と `AUDIT_STOP_NOTICE` を追加
- `withAudit` はシンクの失敗を捕え、**手元の `ToolResult` を載せて**投げる。結果を捨てない
- `serve` は `handleMessage` と `channel.write` を try で包み、**1リクエスト1応答**を守る。
  `RPC_ERROR.INTERNAL`（定義だけあって未参照だった）に初めて使い道が付いた
- `serve` は例外で終わらず `ServeOutcome` を返す。`main.ts` が「起動に失敗しました」と
  「サーバを停止しました」を言い分ける
- `createFileAuditSink` の記述子管理を sonic-boom へ委ね、**時計の注入口**を足した

**結果**: `npm run check` 緑（**403件 pass**。393 → 403 で、増えたのは追加した10件ちょうど）。
`npm run verify:rules` **28/28**。`npm pack` は30ファイル・312 kB で、`dist/mcp/audit.js` に
ベア指定の import が残り、型だけの import は消えている。

**違反プローブを3件打ち、すべて赤になることを確認して戻した。**
①結果を捨てる ②告知を載せない ③応答せずに読み飛ばす。

### 解釈

**3件が同時に落ちていた理由は「うっかり」ではない。この設計には
「稼働中に監査が書けなくなった」という状態が存在しなかった。**
`AuditSink` の型、`multiAuditSink` のコメント（「起動を続けない」）、`@throws {ConfigError}`、
README の「書けなければ起動しない」— すべて**起動時の語彙**で書かれている。
だから4箇所とも書き込みを絶対に失敗しないものとして書かれた。

**`RPC_ERROR.INTERNAL` が定義だけで未参照だったのは、
「1リクエストに必ず1応答」という不変条件が最初から実装されなかった化石。**

**自分の案が、実装者ノートが名指ししている失敗と同じ形だった。**
最初は停止機構を optional の引数で渡す案を出したが、ノート §4 は sonic-boom の `'error'` リスナに
ついて「付け忘れると挙動が変わる。**設定で守るのであって構造ではない**」と書いている。
しかも私はそれを「既存テストが動かないから良い seam だ」と正当化していた。
**例外の型で運ぶ形に変えたら、構造で守れて既存テストも動かない** — 両立した。

**ノートの見立てを1件訂正した。** 「リスナを付ける＝続ける / 付けない＝伝える」は正しくない。
リスナの無い `'error'` は `uncaughtException` になるので、**リスナの有無は
「制御された停止か、既定のクラッシュか」**。裁定が「止める」でもリスナは付けるのが正しい。

### 決定

- **L4-4（`withAudit` の例外経路が未テスト）を根F から根C へ移して閉じた**（人間の承認済み）
- **束ねずに外した**: `server.ts:160` の起動時同型 / `stderrAuditSink` の EPIPE /
  `withAudit.listTools` が何も記録しない（根D で決める）/ DESIGN.md §5 の**理由の書き直し**
  （「監査は消せる口を持たない」を原則として書く案。根E の側）
- **`VERSIONS.md` の「実行時依存 — `got` のみ」は元から偽**（`@dotenvx/dotenvx` と `file-type` が既にある）。
  `sonic-boom` を足して一段偽になったが、**元からあった乖離なので黙って直さず根E へ送った**。
  引き継ぎが申し送っていた「自分の修正が偽にした記述」と「元からあった乖離」を分ける語彙が、
  ここで初めて実際に効いた
- **`'error'` リスナは一度失敗したら以後も失敗させる。** ローテーション成功後の close 失敗のような
  実害の無い失敗でも止まりうるが、fail-closed の側へ倒す（コメントに書いた）

---

## 12:50 — T-1 を裁定し、囲みの書式の方針を確定

### 事実

**T-1 の材料を自分で測った。** 1囲みの定型文は **254 バイト**（L3-5 の報告 252 とほぼ一致。
差は `source` 文字列の長さぶん）。**内訳で注意書きの `<!-- … -->` 行が 146 バイト＝57%**。
`nonce` は 42 バイト。

**注意書きは `instructions` と重複していた**（`server.ts:46` と `untrusted.ts:19-20`。ほぼ同文）。
ただし **`instructions` に寄せることはできない** — MCP 仕様（`schema.ts:688-695`）は
「This **can** be used by clients」と書いており、**根C で stderr を退けたのと同じ形**になる
（`audit.ts:27-29`「耐久性を他人に委ねない」）。

**実装者の見立ては前提が成り立たなかった。** 「1応答で `nonce` を共有すれば定型文の繰り返しを
1回に減らせる」は**1バイトも減らない**（`nonce` は開始タグと終了タグの両方に書くので、
共有しても出力量は同じ。実測: nonce ありの開始＋終了 105B / なし 63B）。
減らせるのは注意書きで、それは `nonce` と独立に外せる。

**段階3 の洗い出しで、守られていないものが1つ出た。** **注意書きの一文をテストが1件も見ていない**
（`grep` で0件）。**いま消しても `npm run check` は緑になる。**

**`pickName` の前提が崩れた。** 根B と実装者コメントの両方が「`pickName` の中で囲む」を
確定済みとしていたが、**`pickName` はユーザー専用ではない** — `issueType` / `status` /
`priority` / `resolution`（管理者が定義した閉じた選択肢）も同じ関数を通る（`tools.ts:677-680`）。

**実装者から照合が来た（`9cbee43`）。自分の案を撤回し、こちらの記述を2件正してきた。**

| 指摘                                            | 検証結果                                                              |
| ----------------------------------------------- | --------------------------------------------------------------------- |
| `CallToolResult.content` はカテゴリが違う       | **引用は正確**（`schema.ts:1811`）。こちらの区別を補強する側          |
| 「配列全体を固定しているテストは無い」は不正確 | **正しい。** `server.test.ts:261`（根C で統合者が足したもの）        |
| 単価 × 個数の**個数側**が手つかず               | **正しい。しかも統合者の基数が接地されていなかった**                 |

3つ目が重い。`search_issues` は `get_issue` と**同じ `shapeIssue`** を使い（`tools.ts:1278`）、
**`summary` と `description` の両方を囲む**。20件なら構造上の下限が40囲み。
**「9,979 は再現していない」と自分で書きながら、そこから出る 20 を効果表の基数に使っていた。**

### 解釈

**囲みのコストは本文の長さではなく囲みの個数に比例する。** 人名のような短い値に 254 バイトを
巻くと 25倍のオーバーヘッドになる。だから T-1 は「広げるか下げるか」ではなく、
**下げてからでないと広げられない**。

**囲みの中身を2つに割ると分類が分かれる**（補遺の「書くべき／書いてもいい／書いてはいけない」）。

- **境界**（`source` ＋ `nonce` ＋ 開始／終了タグ）→ **書くべき**（値ごとに要る）
- **注意書きの繰り返し** → **書いてはいけない**（同じ応答で60回言っても読み手の行動を変えない）
- 注意書きそのもの → **書いてもいい**（ただし in-band で1回）

> **境界は毎回要るが、指示は1回でよい。**

**理由の順序を逆に書いていた。** 末尾（`content[1]`）を選んだ理由を「既存テストが動かないから」と
書いたが、それは**測れる基準を、測れない問いの答えとして提示する**形だった。正しくは
「測れないので、副作用の小さい側を採った」。**このレビューで3回出た誤りと同じ形**
（`git log -S` の3回の誤読・「実測」ラベルの過信）で、今回は実装者に指摘されて気づいた。

**レンズ間の独立が効いた。** 実装者は「ノートが push される前に独立して意見を書き、そのあと読んだ」と
順序を明記している。統合者が見落とした2件は、どちらも**自分の書いたものを自分で検算する形では
出てこなかった**もの。

### 決定

- **T-1 の裁定: 書式を先に下げてから対象を広げる。** 方針は `_review/L3-5-囲みのコストと書式.md`
- **`instructions` には寄せない**（クライアント依存。根C の論法と矛盾する）
- **`nonce` の設計は触らない**（実装者の案は前提が成り立たない）
- **効果として言えるのは単価だけ**（254 → 107、58%減）。**個数側は根B の段で実データを取る**
- **「`pickName` の中で囲む」は確定を取り消し**、`pickUserName` を作る案を根B の段階3 へ持ち越し
- コミット3本を push（`6b358c0` / `89aedfa` ほか）

---

## 13:02 — L3-5 の段階5。囲みの注意書きを応答に1回へ

### 事実

**単価が 254 → 107 バイトになった**（実測）。応答ごとに1回 137 バイトを末尾のブロックに載せる。

`npm run check` 緑。**408件 pass**（403 → 408。増えたのは追加した5件ちょうどで、
**既存テストは1件も動いていない**）。

**方針から1件、意図的に逸脱した。** 方針は「`UNTRUSTED_NOTICE` を `tools.ts` へ移す」と
書いたが、**`untrusted.ts` に置いたまま export した**。段階3 が
「『気休め』の宣言が定数に付いている。動かすと DESIGN §8 と README:456 の指す先が消える」と
洗い出していたので、**動かさなければそのリスクが無い**。

**実装で1つ分かった。** 目印に `"` を含めると当たらない — 成功の応答は `JSON.stringify` を
通るので `<untrusted source=\"` にエスケープされる。目印は `<untrusted source=` までにした。

**違反プローブが1回目は打てていなかった。**

1. **置換が当たっていなかった。** 整形で文面が変わっていたのに `assert` を入れずに打った
2. 打ち直したら2件目が**緑のまま**。原因は**テストが弱かった**こと —
   「囲みを含まない応答には載らない」を `delete_issue`（未知のツール）で書いていたが、
   **未知のツールは早期 return で `withUntrustedNotice` を通らない**

結果を組み立てる経路（件名も本文も無い課題）で書き直したら赤になった。

**`npm run check` が1回だけ `fail 1` を出した。再現しない**（`node --test` 8回・
`npm run check` 2回とも緑）。**失敗名を取り逃がした**ので特定できていない。

### 解釈

**違反プローブを打たなければ、弱いテストのまま緑で終わっていた。**
補遺の「緑は守られている証拠にならない」が、成果物ではなく**テストの側**に当たった。

**そしてプローブ自体にも同じ穴がある。** 置換が当たったかを検査しないと、
**緑が2通りの意味を持つ**（守られている／打てていない）。今回は1回目がそれだった。
**プローブは「当てたこと」を assert してから走らせる。**

**逸脱の判断は段階3 の出力があったから下せた。** 「定数に宣言が付いている」を
書き出していなければ、方針どおり移して、寄りかかっている2箇所を壊していた可能性がある。

### 決定

- **`UNTRUSTED_NOTICE` は `untrusted.ts` に残して export する**（方針からの逸脱として記録）
- **目印は `<untrusted source=` まで**（`"` を含めない）
- **再現しない失敗1件を `02-引き継ぎ.md` に申し送る。** 握りつぶさない。
  疑うなら根C で入れた `sonic-boom` の非同期（`reopen` 成功時に古い記述子を nextTick で閉じる）で、
  **テストスイートに新しく入った唯一の非同期**。次に見かけたら失敗名を必ず控える

---

## 20:07 — 根B。一次情報で分類をやり直し、23箇所を囲んだ

### 事実

**ミラーとライブの公式ドキュメントを引いたら、誤分類は1つの値ではなく分類そのものだった。**

| 値 | 権限 | 判定 |
| --- | --- | --- |
| `priority` / `resolution` | **書き込む口が API に無い** | 固定 |
| `status` | **管理者** | 管理者定義 |
| `issueType` / `category` / マイルストーン | **すべての権限** | **第三者** |
| ドキュメントのタグ / リスト項目の追加 | **すべての権限** | **第三者** |

比較の基準として `add-comment`（第三者が書く典型）も「すべての権限」。
ミラーは `管理者` と `すべての権限` を**書き分けている**。**5件をライブでも照合し、完全一致。**

**`support-ja.backlog.com` が読めた。** `curl` / `defuddle` / `WebFetch` / `node fetch` はすべて
Cloudflare に 403 で弾かれるが、**`python3` の `urllib` は 200**。Zendesk の Help Center API なら
本文と `updated_at` が JSON で取れる。**ユーザーのハンドルネームは本人が変更できる**ことが、
記事「自分のユーザー情報を変更する」（更新日 2026-09-01）の原文で確定した。

**修正**: 23箇所を囲んだ。`npm run check` 緑・**417件 pass**（408 → 417）。
**既存テストは4件動いた**（方針どおり。出力の契約を変えたため）。
違反プローブ3件（囲みすぎ / 囲み漏れ / 識別子を囲む）がすべて赤になることを確認して戻した。

### 解釈

**「一次情報を引く」が、指摘の範囲そのものを変えた。** レンズが挙げたのは1箇所、
横断探索で3箇所、**一次情報で5種類増えた**。推論でも横断でも出なかったものが出た。

**前セッションと私の両方が、道具を変えずに「到達できない」と結論していた。**
`curl` で 403 を見て、そこで止まっていた。DESIGN.md にも「この環境から出られない」と
書かれていた。**403 は「読めない」ではなく「その道具では読めない」だった。**

**L2 の評価を訂正した。** 「一次情報の引用を1件捏造していた」と書いていたが、
**主張は正しく、出典も実在した**。成立していなかったのは「L2 がそれを読んだ」という部分だけ。
較正（独立の裏取りが取れた主張だけ残す）は変えない — **現にその手順が働いて裏取りが取れた**。

**識別子として往復する面は囲まない、という線を引いた。** `lookupName` のエラーメッセージと
`list_project_masters` の一覧は「次の呼び出しに渡す名前」を教える面で、
添付のファイル名（T-2）とまったく同じ構造。**C-2 を根B から T-2 へ移した。**

### 決定

- **囲むのは23箇所。囲まないのは `status`（管理者）と `priority` / `resolution`（口が無い）だけ**
- **`pickName` の中では囲まない** — 過剰になるうえ、唯一の経路という性質は通す形を変えないことで保つ
- **C-2（`lookupName`）と `list_project_masters` は T-2 の裁定に合流**（根B の範囲を狭めた）
- **既存テスト4件の期待値を書き換えた**。根C・L3-5 では1件も動かしていないが、
  今回は**出力の契約を変える修正**なので方針に明記したうえで書き換えた
- **403 を見たら道具を変える**、を `02-引き継ぎ.md` に残した

---

## 20:14 — 第2巡を閉じ、次セッションへ渡す

### 事実

**引き継ぎ文書に齟齬が1件あった。** 「次セッションにすべきか」を答える前に
`02-引き継ぎ.md` を読み直したところ、**「済んだこと」の表に L3-5 と根B が無く**、
**「3件とも既存テストは1件も動いていない（… → 403）」が偽**になっていた。
根B は既存テスト4件を動かしており、件数も 417。直して push（`92f8ed8`）。

**このセッションで閉じたのは3件。**

| | 内容 | ゲート |
| --- | --- | --- |
| 根C | 監査層（L1-3・L1-1・L1-2 ＋ L4-4）。稼働中の fail-closed を裁定し `sonic-boom` を採用 | 403件 |
| L3-5 / T-1 | 囲みの注意書きを応答に1回へ（単価 254 → 107 バイト） | 408件 |
| 根B | 一次情報で信頼分類をやり直し23箇所を囲んだ。C-2 は T-2 へ | **417件** |

`npm run verify:rules` は 28/28 のまま。実行時依存を1つ増やした（`sonic-boom` ＋ `atomic-sleep`）。

**引き継ぎプロンプトを作った。** 前回のプロンプトから変えたのは3点。

1. **罠の項を独立させた** — 「403 を見たら道具を変える」。埋め込むと読み飛ばされる
2. **「既存テストが動く例外」を明記した** — 次に同じ形が来たとき seam の失敗と誤読させないため
3. **推奨を「次の根」ではなく「第3巡の再レビュー」にした**

### 解釈

**引き継ぎ文書は、書いた本人が読み直さないと腐る。** 今回は各段階でこまめに更新していたのに、
**「済んだこと」の要約行だけが取り残されていた** — 表は直したが、その下の一文を見ていなかった。
**表を直したら、その表を要約している文も読む。** 変更した箇所の近傍を通しで読む、の実例。

**修正を積むより再レビューを挟むほうが筋。** 第2巡として提案されていたのは根A・B・C で、
5件はそれを超えている。しかも `src/tool/tools.ts` は第1巡のレンズが見た版とは別物になった。

**このセッションで最も効いた較正は「403 を見たら道具を変える」だった。**
指摘の質でも手法の適用でもなく、**道具の当て方**。同じ形の誤りが3回出ている
（`git log -S` の誤読・「実測」ラベルの過信・403 で止まる）が、いずれも
**道具の出力を、その道具が答えていない問いの答えとして読んでいた。**

### 決定

- **第2巡はここで閉じる。** 次は第3巡の再レビューを推奨（修正を続けるなら根F）
- 引き継ぎプロンプトはユーザーへ渡した。**推奨の段落だけ差し替えれば根F 版になる**
- 未解決は2件のまま申し送り — 再現しない `fail 1`（失敗名を控えること）と、
  `VERSIONS.md` の「実行時依存 — `got` のみ」が元から偽（根E へ）

## 20:39 — L4-5 に独立して意見を書いた（却下）

### 事実（調べたこと）

`_review/` に L4-5 のノートがまだ無い段階で、独立に見た。台帳は重要度 L・根F に分類している。
結果を `01-実装者コメント.md` の §9 に置いた。

**行番号を確認した** — `config.ts:128`（L1 のような系統的なずれは無い）。

| 確かめたこと | 結果 |
| --- | --- |
| 保険の分岐に到達できるか | **できない。** dotenvx が正常なら通らない |
| 契約テストの有無 | **ある。** `tests/apiKey.test.ts:89`「別の鍵ペアでは送出する」が**実ライブラリ**で固定 |
| `ConfigOverrides` から到達できるか | **できない。** `resolveApiKey` は `decryptApiKey` を丸ごと差し替える（`config.ts:286`） |

### 解釈（却下の理由）

**「未テスト」ではなく「到達不能」。** カバレッジ計測は「まだ書いていない」と
「意図的に到達しない防御コード」を区別できないので、L4 は機械的に拾っている。

**守りたい契約はすでに埋まっている。** `apiKey.test.ts` はモックを使わず、
`set` で実ファイルを暗号化して鍵を取り違える。ファイル冒頭が
「守りたい性質がライブラリの振る舞いに乗っており、上流のテストがその組み合わせを
カバーしていない」と理由まで書いている。

**到達させると API キーの経路に差し替え口が増える。** `loadEnvFile` を注入可能にする必要があり、
`ConfigOverrides` の TSDoc が掲げる「設定として危険な値を表現できるようにはならない」と
向きが逆になる。**しかもその口で書けるテストは「自分の `if` が動く」ことしか示さない**
（証明したいのは「dotenvx がそう返しうるか」で、そちらは契約テストが担っている）。

### 事実（一度考えて、取り下げた案）

「契約テストがあるなら保険は冗長では」と考えたが、**走る場所が違った**。
契約テストは**開発環境で `npm run check` のとき**、保険は**利用者の環境で毎回の起動時**。
dotenvx が回帰しても、利用者側で `npm install` が走った経路ではテストは一度も走らない。
**保険はそこにしか居ない最後の一枚。** 消さない。

### 解釈（根F の切り分け）

台帳は L4-3・L4-5 を根F にまとめているが、**L4-3 は到達可能・L4-5 は到達不能**で性質が違う。
`02-引き継ぎ.md` が根F を「テストだけで、コードを触らない安全な作業」と書いているので、
**一括で進めると L4-5 で差し替え口を作ってしまう。** 分けるよう申し送った。

`npm run check` 緑（417件）。

## 20:52 — 巡の数え直しと、根F（L4-3・L4-4 の残り・L4-5）

### 事実

**巡の用法が2通りになっていた。** 台帳のフォーマットは `## 第N巡（日付・版・視点一覧）` と
**視点で巡を定義**しているのに、収束節（台帳:209）から**修正作業を「第2巡」と呼ぶ**用法に切り替わり、
`00-現在地.md` と `README.md` が追随していた。**回ったレビュー巡は第1巡だけ。**
人間の裁定で数え直し、4ファイル5箇所を訂正した（訂正の経緯も台帳に残した）。

**根F を段階1〜5 まで通した。** カバレッジで未到達を実測し、L4 が挙げた位置は4件とも動いていた
（根B で masters.ts が +71行）。

| 指摘 | 結果 |
| --- | --- |
| L4-3 | masters のテスト4件。**カバレッジ 100%** |
| **L4-4 の残り半分** | `parseRequest` は根C で閉じたつもりだったが**未到達のままだった**。テスト2件 |
| L4-5 | **却下**。dotenvx の strict 下で到達経路が無い。保険は残す |

`npm run check` 緑・**424件 pass**（417 → 424、足した7件ちょうど）。**既存テストは1件も動かず。**
違反プローブ6件はすべて `fail 1`（分岐を潰すと対応するテストだけが赤くなる）。

**実装者セッションが L4-5 へのコメントを push していた**（`b965d9b`）。独立に同じ結論（却下）に
到達しており、引用4件はすべて正確だった。**私の裏取りに無かった論点**を1つ持っていた —
契約テストは開発環境でしか走らないが、**保険は利用者の環境で毎起動走る**。

### 解釈

**自分の修正の側に、同じ種類の欠陥を2件作った。**

1. 追加したテストが `assert.throws(..., ConfigError)` でクラスしか見ておらず、
   **上流が回帰すると保険が同じ ConfigError を投げるので緑のまま通る**。
   「上流の回帰を検知する」というテストの主張自体が成立していなかった
2. 送出の出所を「後検査（`unresolvedEncryptedErrors`）」と帰属したが、
   **その関数を潰しても送出は変わらなかった**。出所は特定できていない

2件目は台帳の較正が挙げる形（道具が答えていない問いの答えとして読む）の**4度目**。
**指摘の接地は済ませたのに、自分が書いた側の接地を飛ばしていた。**

**`npm run check` の exit 1 を2回見たが、どちらも `prettier --check` だった。**
申し送り①（再現しないテスト失敗）は今日の追試では再現していない。
`node --test` の要約は `ℹ fail` で、TAP の `# fail` ではない — **`| tail` に通すと件数行ごと消える**。

### 決定

- **巡はレビュー巡を数える。** 修正は巡の中の工程。次に再レビューを回せば**第2巡**
- **L4-5 は却下し、保険は残す。** 代わりに①上流の振る舞いを実ファイルで1件固定
  ②`config.ts` に「なぜ意図的に未到達のまま置くか」を記述
- **却下理由は台帳側で自己完結させる**（台帳から `_review/` を参照しない。人間の裁定）
- **修正中に見つけた欠陥**（全員の名前が衝突すると、空でない応答に「応答が空です」で起動ごと落ちる。
  `5055619` から在る）は**台帳に新規 ID を立てず、修正側の記録に残す**（人間の裁定）。
  **テストでもその文面を固定しない**（実態と食い違うため）

## 21:09 — T-2 に独立して意見を書いた（枠組みがずれている）

### 事実（測ったこと）

`_review/` に T-2 のノートが無い段階で、攻撃者が制御できる添付ファイル名を実際に通した。
結果を `01-実装者コメント.md` の §10 に置いた。

**同じファイル名が、経路で扱いが正反対だった。**

| 経路 | 第三者の名前が入る形 | 囲み | 注意書き |
| --- | --- | --- | --- |
| 一覧の `name`（T-2 の対象） | `JSON.stringify` で**エスケープ済み**。構造は壊せない | 無し | **付く** |
| `findAttachment` の失敗 | **素のテキスト。改行が生きる** | 無し | **付かない** |

`callTool` の catch は `text: message` を**そのまま**返す（`tools.ts:2578-2579`）ので、
`findAttachment` が組み立てた候補**最大20件**が地の文で出る。
`withUntrustedNotice` は「応答に `<untrusted source=` が含まれるか」で判定するため、
**囲みが1つも無いエラー応答には注意書きも付かない。**

### 事実（台帳の記述が1つ成り立たない）

台帳は「同じ判断が `list_wiki_pages` のページ名にも掛かっている」と書いているが、
`findWikiId` が載せる `name` は **LLM 自身が渡した引数**で、候補も列挙しない。**Wiki は対象外。**

一方 `lookupName`（`masters.ts`）は**最大20人ぶんの表示名**を同じ素の経路に載せる。
根B で表示名を「第三者が書ける」に分類し直したのに、**この経路は囲みの外**。

### 解釈

**T-2 が心配している場所は、いちばん危ない場所ではない。**

一覧の `name` は囲まないままでよい — 囲みは DESIGN.md §8 が「対策済みとして数えない」と
宣言しているもので、**数えないものを付けるために識別子としての機能を壊すのは割に合わない**。
構造的なリスクは JSON エンコードが既に潰している。

**直すべきはエラー経路で、これはトレードオフではなく根A と同型の欠陥。**
保護が正常系にだけ貼られ、別の経路が迂回している。`guardGateway` でやったことが要る。

**つまり T-2 は2つに割れる** — 人間の裁定が要るのは「一覧の `name` を囲むか」だけで、
エラー経路のほうは裁定を待たずに直せる。

### 残した論点

具体案（候補の列挙だけを囲む）は**未検証**。`findAttachment` は `tool/` 層なので `wrapUntrusted` を
呼べるが、**`lookupName` は `domain/` 層で §4 の語彙表では `untrusted` を知ってはいけない**
（根A で案2 を却下したのと同じ制約）。**同じ手が使えないので seam の置き場所が論点になる。**

`npm run check` 緑（424件）。

## 21:42 — T-2 を仕様で裁定し、エラー経路の候補列挙を囲んだ

### 事実

**MCP 仕様を引いて T-2 の枠組みが変わった。** ミラーとライブが全文一致（`/specification` は
`2026-07-28` へ転送＝現行版）。

| 出典 | 原文 |
| --- | --- |
| `server/tools.md` Security Considerations | Servers **MUST** … `Sanitize tool outputs` |
| 同 Error Handling | `Clients **SHOULD** provide tool execution errors to language models` |
| `schema/2026-07-28/schema.ts` | `ToolAnnotations` は4つのみ。信頼を表す項目は無い |
| GitHub SEP #1561 / #1487 | `unsafeOutputHint` / `trustedHint` は **2026-06-26 に dormant で closed** |

**エラー経路の文言は「LLM に届くことが前提」**と仕様が定めていた。T-2 の未検証項目
（クライアントがどう見せるか）に、`isError` の応答については答えが出た。
一方 **DESIGN.md §6 の「仕様に入ったら乗り換える」は古くなっていた**（根E へ8件目）。

**横断探索**: `src/` の `throw` を32箇所分類し、第三者の文字列が素のテキストに載るのは
`findAttachment` と `lookupName` の**2箇所だけ**と確定。実装者コメントの指摘と一致した。

**裁定（人間）**: ①正常系は囲まない ②Wiki は対象外 ③エラー経路は直す。
③の実装は当初 JSON 化を推したが、**`guardGateway` が既に「囲み」で同じ問題を解いていた**ので
囲み（③-b）に決まった。

**実施**: `MasterDataError` に候補を構造で持たせ、`tool/` 層に `guardCandidates` を置いた。
`npm run check` 緑・**430件**（424 → 430）。既存テストは**予測どおり1件だけ**動いた。
違反プローブ3件（seam 素通し / 囲み外し / **囲みすぎ**）がすべて赤になることを確認。

### 解釈

**推奨を出した後で、その根拠を崩す材料が出た。** `guardGateway` の先例は、裁定を仰いだ時点で
まだ読んでいなかった。**段階2 を「仕様を読む」で終えたのが早すぎた** — 成果物の中の先例を
探すのは同じ段階2 の仕事で、そちらが後回しになった。

**`guardGateway` の TSDoc は既に偽だった。** 「`planToolCall` が投げるのはこちらが書いた文言」は
`findAttachment` が候補を並べる時点で成り立っていない。**私の修正が偽にしたのではなく、
元からの乖離**（申し送りの語彙が2度目に効いた）。

**新しい seam は `guardGateway` より弱い。** あちらは「層で分けているからエラー名の一覧が要らない」
という構造的な保証だが、`guardCandidates` は `candidates` の有無で判定する。
**第三者由来のエラーを足す人がその口を使わなければ素で出る。** 弱さを `contract.ts` に書いた。

### 決定

- **囲むのは「第三者が書ける名前」だけ。** LLM 自身が渡した引数（Wiki のページ名）は囲まない
- **注意書きの述語は変えない。** ③-b を採ったことで囲みが入り、結果として付くようになった
- **①の裁定の所在を `shapeAttachment` のコメントに残す**（次に読む人が蒸し返さないため）

## 22:07 — 根D。schema に起動時の事実を差し込んだ

### 事実

**錨が2つあった。** `toDefinition` は既に `attachable` で schema を変えており（`withoutFileProperty`）、
`projectKeysFor(policy, toolName)` も既にあった。さらに `DESIGN.md:109` が
「`tools/list` とハンドラが**同じ集合を参照する**のでズレようがない」と書いている。
**新概念はゼロで、既存の形の一般化で足りた。**

**症状は2件だが枝は5つあった。** 添付（実装済み）／許可キー（L3-1）／priority・resolution（L3-7）／
**プロジェクトごとに違うマスタ（表現できない）**／件数上限（定数なので別種）。

**人間の指摘で範囲が変わった。** 「プロジェクトキーはプロジェクト名と一緒に渡さないとだめでは」。
確かに `masters` は名前を捨てており、キーだけでは「営業の課題」から `SALES` を選べない。
名前の権限をミラーとライブで照合したところ **管理者 / プロジェクト管理者**（完全一致）で、
根B の軸では `status` と同じ管理者定義側。**囲まずに出せる。**

`enum` は値の配列しか持てない（JSON Schema 2020-12 §6.1.2 を一次情報で確認）ので、
**検証は `enum`・案内は `description`** に分けた。同じ関数が両方を作るので乖離しない。

**実施**: `Masters` に `projectNames` を足し、`withRuntimeFacts` を置いた。
`npm run check` 緑・**436件**（430 → 436）。**既存テストは1件も動かず**（予測どおり）。
違反プローブ3件（enum を落とす / **ポリシーに無いキーを混ぜる** / 名前の対応表を落とす）が
すべて赤になることを確認。

### 解釈

**「規約に enum 禁止と書いてある」で止まらなくてよかった。** 指摘を受けて §4.1 を読み直すと、
禁じているのは **TypeScript の `enum` 宣言**（`verify:rules` のプローブも `export enum Color` を検査）で、
**JSON Schema の `enum` は同じファイルで既に使われていた**（`sort` / `order`）。
**規約の適用範囲を、語が同じというだけで広げない。**

**「JSON に enum なんてあるの?」が正しい問いだった。** JSON 自体には無い。あるのは JSON Schema の
検証語彙で、実体は JSON から見ればただのキーと配列。**説明を一段下げるまで、私は用語で誤魔化していた。**

**プロジェクト名の指摘は、レンズも実装者コメントも挙げていない。** L3-1 は「キーを知る手段が無い」
までしか書いておらず、**キーを知っても選べない**ことは書いていなかった。

### 決定

- **`enum` は「ツール単位で1つに決まるもの」だけに入れる。** プロジェクトごとに違うマスタは
  `list_project_masters` の面として残す
- **件数の上限は書かない。** 定数であり、書くと「サーバが切り下げて申告する」（§5.4）と挙動が変わる
- **`toDefinition` に `context` ごと渡さない。** 生の `gateway` を持つので、`policy` と `masters` だけ渡す（根A）
## 22:21 — L4-2 に独立して意見を書いた（#2 と #6 の順序が決まる）

### 事実（測ったこと）

根E #2（層と語彙の表に `attach/` が無い）は段階3 が未着手だったので、
`src/libs/` を除く**17ファイル全部**の内部 import と外部依存を取ってから書いた。
結果を `01-実装者コメント.md` の §11 に置いた。

**表に無いのは4つ** — `attach/` / `config.ts` / `server.ts` / `main.ts`。
根E の「欠落は `attach/` だけではない可能性」は**当たっていた**。

**lint の境界は3層にしかない**（`tool/` `policy/` `domain/`）。`attach/` `mcp/` `shared/` と
`config.ts` / `server.ts` / `main.ts` はどこからでも import できる。

**表に書いていない依存が1本**: `tool/tools.ts:31` が `mcp/protocol.ts` を型 import している。
`buildHandlers` が `McpHandlers` を返すので依存としては正しいが、**表がそれを言っていない。**

### 解釈（いちばん重要な点）

**`attach/` の語彙は、すでに DESIGN.md に書いてある。**

> `DESIGN.md:440`「この順にするのは、**1 が型システムの外側（ファイルの中身）を扱う唯一の層**だから。」

実測とも一致する（`file-type` を使う唯一の場所、`node:fs/promises` を開く唯一の場所）。
**そして根E はこの文を #6 として「消す」に分類している。**

**#6 を先に当てると #2 が要る錨が消える** — 補遺の実例3（例が暗黙に前提を担っていた＝消しすぎ）
そのものの形。**同じ一文に2つの項が依存しているので、順序が決まる**（#2 → #6）。

### 事実（`config.ts → attach/` の正体）

根E が「表のどの層にも属さない」と指摘した依存は、**モジュールの置き場所の問題**だった。
`isInside` / `resolveAttachmentRoot` はどちらも `node:path` だけの純関数で、
**ファイルを触らない**。`attach/`（ファイルの中身を扱う）の定義に当てはまらない2つが
同じファイルに居るだけ。**`shared/` へ移せば辺が消え、表に例外行を足さずに済む。**

### 解釈（表だけ足しても足りない）

lint の境界を `attach/` にも足さないと、**表が宣言になる**。§8.2 が「一部を機械強制側へ
持ってきている」と言う以上、その「一部」の範囲は表と lint の両方で示す必要がある。
根A で学んだ「文書に書いてあるが機械が守っていない」形。

`npm run check` 緑（424件）。

## 22:35 — 根E。文書の乖離10件と、表に無かった層

### 事実

**段階3 で、指摘より範囲が広いことが分かった。** `src/` の import を機械的に取り出して
DESIGN.md §4 の表7行に当てたところ、**表に無いのは `attach/` だけでなく
`config.ts` / `server.ts` / `main.ts` の4つ**で、**lint の境界は3層にしかない**（表は7層分の禁止を書いている）。

**grep では出ない乖離が1件出た。** `masters.ts:306` の TSDoc「書き込みを許したプロジェクトでしか
呼ばない」は偽で、**同じファイルの343行と矛盾**していた。「全プロジェクト」で検索しても出ず、
近傍を通しで読んで出た（補遺の実例6 の形）。

**README の横断で2件。** `README:287`「どれも管理者が付けた名前で、第三者の自由記述ではない」は
**偽**（C-2 と同型。TSDoc は根B で直したが README は未修正）で、**同じ README の囲みの表と矛盾**。
もう1件は囲みの表の「裁定は保留」で、**今日の T-2 で私が偽にしたもの**。

**実装者コメントが順序制約を指摘した。** `DESIGN.md:440`「1 が型システムの外側（ファイルの中身）を
扱う唯一の層だから」は #6 で「消す」分類だが、**この一文が `attach/` の語彙そのもの**。
**先に消すと #2 の錨が失われる**（補遺の実例3）。統合者の段階3 はこの依存を見ていなかった。

**実施**: 10手順を順序どおり当てた。`shared/paths.ts` を新設して純関数2つを移し
（`config.ts → attach/` の辺が消えた）、表に `attach/` と組み立て役の説明を足し、
lint の境界と `verify:rules` のプローブを追加。`npm run check` 緑・**436件（不変）**・
**verify:rules 29/29**。

### 解釈

**テスト件数が変わらないことが、移動の正しさの証拠になった。** 挙動を変えていないので当然だが、
「1件も動かない」を予測して当てた点は seam の判定と同じ形。

**「表に足すだけでは宣言にしかならない」**（実装者コメント）。根A で学んだ「貼り忘れられる形をやめる」
と同じ話が、文書と機械強制の間にもある。**表を足したら lint も足す。**

**手順9 が2回連続で効いた。** T-2 で `guardGateway` の TSDoc、根E で README の「裁定は保留」。
どちらも**自分の直前の修正が偽にした記述**で、探しに行かなければ残っていた。

### 決定

- **`shared/` の記述を「言語機能と純粋な計算」へ広げる。** 広げた先はパス計算だけで、
  I/O は「知ってはいけない」側に明記した
- **組み立て役（`server.ts` / `main.ts` / `config.ts`）は層として数えない。** 表に無いことを
  「欠落」と読まれないよう本文に書く
- **`shared/` と `mcp/` の lint 境界は入れない**（L4-2 の範囲外・違反は現状ゼロ）。修正側の記録に残す

## 23:16 — H 3件（L3-2・L3-3・L3-6）と、調査で出た新規1件

### 事実

引き継ぎの順序1（H 3件）を段階1〜5 まで通した。**根はすべて閉じていたので、残るのは単発と
description 側**という前提で入った。

**L3-2**（`get_issue` の description がカスタム属性について偽）。不変条件へ裏返して
「応答の中身について述べている記述は、実装が返すものと一致する」で sweep したら、
**症状の言葉では1本だった枝が4本**になった。

| 場所 | 偽にした変更 |
| --- | --- |
| `contract.ts:125` の description | `8fb1334` |
| `tools.ts` の `shapeIssue` TSDoc（畳む表・「中身を返すかは別の判断」） | `8fb1334` |
| `README.md:409` の畳む表 | `8fb1334` |
| **`README.md:442`「`{id,name}` は囲まない」** | **`e11ac07`＝前セッションの根B** |

**4件目は前セッション自身の二次被害。** `e11ac07` は `src/` と `tests/` しか触っておらず、
**手順9 が README まで届いていなかった**。README:465 の新しい表は「囲む」と書いており、
**同じファイルの20行違いで自己矛盾**していた。

**L3-3**（`additionalProperties: false` を宣言しつつ未知の引数を無視）。probe で実行確認 —
`get_issue` に `{issueKey:'PROJ-1', projectKey:'SECRET', typo:1}` を渡すと `isError: undefined` で成功した。
MCP 仕様 `server/tools.md` の Security Considerations が Servers **MUST**「Validate all tool inputs」
（2026-07-28 版・ミラーで確認）。

**L3-6**（annotations が実挙動と一致しない）。authoritative schema で `readOnlyHint` =
「the tool does not modify its environment」／`destructiveHint` は `readOnlyHint == false` の
ときだけ意味を持つ、を確認。

**調査中に probe で新規1件が出た。** `BACKLOG_DOWNLOADS_DIR` だけ設定して
`BACKLOG_ATTACHMENTS_ROOT` を設定しないと、`get_issue_attachment` が
`required: ["issueKey","file"]` なのに `properties` に `file` が無く
`additionalProperties: false` という**呼べない schema** で advertise される。

ゲート: `npm run check` 緑（**453件** pass。436 → +17）／`verify:rules` **29/29**。
**既存テストが動いたのは L3-6 の1件だけ**（`destructiveHint === false` を全ツールに表明していたもの）。

### 解釈

**「なぜ1箇所だけ落ちたか」を型で説明できた件が2つあった。**

- L3-3 — 引数の検査が読み取り関数（`requiredString` など）に埋まっている。読み取り関数は
  **読んだキーしか見えない**ので、読まなかったキーを検出できる位置にいない。**うっかりではなく、
  その位置には置けない。** 錨は `policy.ts:123` の「未知の項目」にあり、新概念ゼロで済んだ
- L3-6 — `ToolSpec.readOnly` は `boolean` 1つで**2つの問い**に答えている（「Backlog を書き換えるか」と
  「環境を変えるか」）。消費者も2つ（annotations と `writableProjectKeys`）。`get_issue_attachment` は
  **前者 false・後者 true という唯一の組み合わせ**なので、そこだけ落ちた。
  **だから `readOnly` を反転させて直すのは誤り**だった

**既存テストの題が根の説明を持っていた。** `tools.test.ts` の
「annotations を全ツールに付ける（**既定の destructiveHint: true を避ける**）」— つまり
`destructiveHint: false` は**既定値に対して選ばれた値**で、ツールごとに測った値ではない。
手順2 の「なぜ落ちたか」に、テストの題が直接答えた。

**修正が自分の主張を偽にしかけた。** `readOnlyHint` を false にすると `idempotentHint` が
**そこで初めて意味を持つ**。`spec.readOnly` のままだと `get_issue_attachment` に
「繰り返しても影響が増えない」と言わせることになるが、`openFresh` は `name-2.pdf` と増やす。
**手順9 を修正の最中に適用する必要があった**（後からではなく）。

### 決定

- **L3-2 の4件目（README:442）を束ねた**（人間の裁定）。同じ不変条件・同じ sweep で出た記述のみの修正
- **L3-3 の照合先は宣言のもと（`INPUT_SCHEMAS`）**（人間の裁定）。advertise 後の schema と照合すると、
  添付未設定時の `file` が「未知の引数」に化け、`BACKLOG_ATTACHMENTS_ROOT` を名指しする案内が消える
- **L3-6 は `ToolSpec` に `writesLocalFile` / `destructive` を必須で足した**（人間の裁定）。
  違反プローブで **TS2741**（書き忘れがコンパイルエラー）を確認
- **新規1件は別コミットで、L3-3 の前に当てた**（人間の裁定）。L3-3 の照合先を決めるのに先に要る
- **`enum` は L3-3 で見ない。** ポリシーとマスタが候補付きのエラーを返しており、先に弾くと案内が消える
- **束ねずに外した**: L3-9 / L3-14（同じ `TOOL_SPECS` だが根が違う）・L3-12（`callTool` の同じ場所だが
  `protocol.ts` の層まで届く別の変更）
