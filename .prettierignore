# 規約 §8.3「借り物・生成物は lint / typecheck の対象から外す」
# 抑制コメントは借り物には使えない（同期や再生成で上書きされる）ため、ここで範囲を切る。

node_modules/
dist/

# 生成物 — 外部ドキュメントのミラー
# Backlog API は tools/backlog-docs、MCP 仕様は tools/mcp-docs で再生成する。
docs/reference/

# 借り物 — 正本は aokazaki-olp/coding-rules
CODING_RULES.md

# 借り物 — スキルの正本は .agents/skills（さらに元は別リポジトリ）
.agents/
.claude/

# 借り物 — ミラー生成スクリプト
tools/

# 借り物 — 正本は aokazaki-olp/libraries（ローカル編集禁止）
src/libs/

# ローカル設定 — 共有しないので検査もしない（`.git/info/exclude` で追跡から外している）
# 追跡している `backlog-policy.example.json` は対象のまま残す。
/backlog-policy.json

# Obsidian vault — Obsidian が書く場所であって、こちらが整形する対象ではない
# （表の桁揃えや箇条書きの記法を書き換えると、Obsidian 側の表示と食い違う）
obsidian/
