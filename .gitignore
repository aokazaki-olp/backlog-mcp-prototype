node_modules/
dist/
*.log

# npm pack の出力（配る実体。ビルドし直せるのでコミットしない）
*.tgz

# tools/sync-libs.sh が使う一時ディレクトリ（借り物の .ts ソース）
.libs-src/

# ローカル設定・秘密
.env
.env.*
backlog-policy.local.json

# 監査ログ（既定の出力先は <policy dir>/logs/。ポリシーをリポジトリ直下に置く場合に当たる）
logs/
audit-*.jsonl
