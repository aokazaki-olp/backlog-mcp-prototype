# 引き継ぎ — `src/` 多角レビューの対応作業

**このファイルは道案内と約束事だけを持つ。** 状態そのものは `00-現在地.md`、
指摘の一覧と却下理由は台帳、根ごとの調査は `根*.md` にある。**重複させない。**

- ブランチ: `feature/track-src-review`（`origin` と同期済み。**PR は作っていない**）
- レビュー対象の版: `5055619`
- ゲート: `npm run check` **緑（436件 pass）** ＋ `npm run verify:rules` **29/29**

---

## 1. 読む順序

```
1. このファイル                      約束事と落とし穴（先に読む）
2. _review/00-現在地.md              根ごとの段階・考察・人間の判断待ち
3. obsidian/ai/reviews/2026-09-06-…台帳.md   指摘36件＋2件、却下理由、収束判定
4. _review/根*.md                    着手する根のものだけ
5. _review/01-実装者コメント.md      実装セッションからの入力（未検証として扱う）
```

**第2巡（再レビュー）を回すときは `_review/lenses/README.md`。**
第1巡の4視点は**文脈を保持したまま復帰できる**ことを実測済み（2026-09-08）。
**エージェント ID はそのファイルにしか無い。消すと文脈保持の経路が使えなくなる。**

**手法の正本は `_review/borrowed/`**（3本）。`.claude/skills/` には**置かない**
（置いた瞬間にスキルとして自動検出されるため。`borrowed/README.md` にその理由が書いてある）。

| ファイル                                   | 何を定めるか                                       |
| ------------------------------------------ | -------------------------------------------------- |
| `multi-angle-review.SKILL.md`              | どの指摘を採るか（統合まで）。**借りもの・実験的** |
| `multi-angle-review.指摘修正のあり方.md`   | 採った指摘をどう落とすか（段階1〜5 の枠）          |
| `multi-angle-review.指摘をコードで直す.md` | その段階2・3・5 をコードに当てた中身（9手順）      |

**`_review/borrowed/` の正本は `~/projects/backlog-mcp/_tmp/borrowed/`。ここでは編集しない。**
更新するときは正本を直してコピーし直し、`diff -r` で差分ゼロを確認する。

---

## 2. 済んだこと

|                | 内容                                                                       | 結果                       |
| -------------- | -------------------------------------------------------------------------- | -------------------------- |
| 第1巡          | 4視点を並列・独立に（L2/L4 は別モデル）。36件 ＋ 統合者が横断探索で2件追加 | 台帳へ                     |
| 根の導出       | **`src/` 5,577行を通読して導出**（最初は推論でやって10のうち4つ外した）    | `00-現在地.md`             |
| **根A（C-1）** | 添付ダウンロードで `<untrusted>` の囲みが外れる経路                        | **修正済み** `90c6bbc`     |
| **L1-4**       | 担当者の名前が別人に解決される                                             | **修正済み** `fa9b771`     |
| **根C**        | 監査層（L1-3・L1-1・L1-2 ＋ L4-4）。稼働中の fail-closed を裁定            | **修正済み**（2026-09-07） |
| **L3-5 / T-1** | 囲みの注意書きを応答に1回へ（単価 254 → 107 バイト）                       | **修正済み**（2026-09-07） |
| **根B**        | 一次情報で信頼分類をやり直し、23箇所を囲んだ。**C-2 は T-2 へ移した**      | **修正済み**（2026-09-07） |
| **根F**        | L4-3（4枝）＋ **L4-4 の残り半分**をテスト6件で閉じ、**L4-5 は却下**        | **修正済み**（2026-09-07） |
| **T-2**        | 裁定を3つに割り、**③エラー経路の候補列挙を囲んだ**。①②は記録のみ           | **修正済み**（2026-09-07） |
| **根D**        | schema に起動時の事実（許可キー・プロジェクト名・優先度）を差し込んだ      | **修正済み**（2026-09-07） |
| **根E**        | DESIGN.md・README・TSDoc の乖離10件と、`attach/` の lint 境界              | **修正済み**（2026-09-07） |
| 手法の抽出     | C-1 の直し方を9手順に                                                      | `9c46d13`                  |

テスト件数は 379 → 389 → 393 → 403 → 408 → 417 → 424 → 430 → **436**。

**根B までの4件は既存テストを1件も動かしていない。根B は4件動いた** —
**出力の契約を変える修正**なので方針に明記したうえで書き換えた（補遺が挙げる例外）。
**「既存テストが動く＝ seam を疑う」が当てはまらない唯一の型**なので、次に同じ形が来たら思い出すこと。

根C では実行時依存を1つ増やした（`sonic-boom` ＋ `atomic-sleep`。理由は `VERSIONS.md`）。

---

## 3. 残っていること

### 人間の判断待ち（**これが決まらないと進まない**）

| #       | 判断                                                                                                                        | 止まっているもの                                      |
| ------- | --------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| ~~T-1~~ | ~~囲む対象を広げるか、囲みのコストを下げるか~~ **裁定済み（書式を先に下げる。2026-09-07）**                                 | `L3-5-囲みのコストと書式.md` に方針。**根B はその後** |
| ~~T-2~~ | ~~「次の呼び出しに渡す識別子」を囲むか~~ **裁定済み（2026-09-07）。①囲まない ②Wiki は対象外 ③エラー経路は直す（実施済み）** | `T-2-識別子として往復する面.md` に根拠と実施記録      |
| ~~—~~   | ~~稼働中に監査ログが書けなくなったら、止めるか続けるか~~ **裁定済み（止める。2026-09-07）**                                 | ~~根C~~ **解決**                                      |
| ~~—~~   | ~~`attach/` に与える語彙~~ **裁定済み（2026-09-07）**                                                                       | ~~根E の #2~~ **解決**                                |

**下2つは設計の決定**で、DESIGN.md に書かれていない空白。どちらを選んでも文書に書く対象になる。

### 着手できる根

| 根    | 内容                                                | 段階       | 備考                                                           |
| ----- | --------------------------------------------------- | ---------- | -------------------------------------------------------------- |
| ~~B~~ | ~~ユーザー表示名の信頼分類~~ **完了（2026-09-07）** | **5 完了** | 一次情報で分類をやり直し23箇所を囲んだ。**C-2 は T-2 へ移動**  |
| ~~C~~ | ~~監査層~~ **完了（2026-09-07）**                   | **5 完了** | `sonic-boom` を採用。`npm run check` 緑・`verify:rules` 28/28  |
| ~~D~~ | ~~schema が静的~~ **完了（2026-09-07）**            | **5 完了** | L3-1・L3-7。**プロジェクト名も載せた**（キーだけでは選べない） |
| ~~E~~ | ~~DESIGN.md の乖離~~ **完了（2026-09-07）**         | **5 完了** | 10件。`attach/` の行と lint の境界を追加（29/29）              |
| ~~F~~ | ~~エラー経路のテスト欠損~~ **完了（2026-09-07）**   | **5 完了** | L4-5 は却下。**L4-4 は前半（`parseRequest`）が未到達だった**   |

### 未対応の指摘 — **20件**（2026-09-07 時点で全件を突き合わせた）

**根はすべて閉じたが、単発と description 側が残っている。** 台帳の状態列は全件「未決」のまま
（**解消の判定はレビュー側の仕事**なので、直した側が触らない）。

#### 修正推奨のまま未対応（11件）

| ID       | 重    | 内容                                                                          | 根   |
| -------- | ----- | ----------------------------------------------------------------------------- | ---- |
| **L3-2** | **H** | `get_issue` の description が「カスタム属性を返さない」と言うが**返している** | E    |
| **L3-3** | **H** | `additionalProperties: false` を宣言しつつ未知の引数を黙って無視              | 単発 |
| **L3-6** | **H** | `readOnlyHint: true` のツールがディスクへ書く／`destructiveHint: false` 固定  | 単発 |
| L1-5     | M     | `SuppressedError` の `suppressed` を辿らない（規約 §6.3）                     | 単発 |
| L1-8     | M     | `assignedToMe` と `assignee` の同時指定で前者が黙って上書き                   | 単発 |
| L1-9     | M     | `/issues/count` の失敗で検索結果ごと捨てる（`Promise.all`）                   | 単発 |
| L1-10    | M     | 添付を上げ切ってから `relatedIssueKey` のポリシー判定                         | 単発 |
| L3-8     | M     | `search_documents` に `projectKey` も識別子も無い                             | 単発 |
| L3-9     | M     | Wiki とドキュメントの使い分けが description から不明                          | E    |
| L3-12    | M     | 未知のツール名を `isError` で返す（仕様は Protocol Error）                    | 単発 |
| L3-14    | L     | 共有 `file` プロパティの説明が「1コメントにつき1件」                          | E    |

**`L3-2` / `L3-9` / `L3-14` は根E の取りこぼし。** 台帳はこの3件も根E に分類しているが、
**根E で扱ったのは DESIGN.md の乖離だけ**で、**ツールの description 側は手つかず**。
`L3-2` は実物で確認済み（`contract.ts:125` は「中身は返さず件数だけ」と言うが `tools.ts:728` は
`customFields` を返している）。

**`L3-12` は MCP 仕様で裏取り済み** — `server/tools.md` の Error Handling が
`Unknown tool` を **Protocol Error** に分類している（2026-09-07 確認）。

#### 保留（統合者の推奨。**人間の裁定が未記録**）（7件）

| ID    | 重  | 内容                                                              |
| ----- | --- | ----------------------------------------------------------------- |
| L1-7  | M   | 同名添付が複数あると先頭を黙って選ぶ                              |
| L3-4  | M   | `offset` が `search_issues` にしか無い                            |
| L3-11 | M   | `instructions` の「確認してください」が広すぎる                   |
| L1-11 | L   | 打ち切りがサロゲートペアを分断する                                |
| L3-13 | L   | `hasParent: true` から親へ辿れない                                |
| L3-15 | L   | 内部エンドポイントパスがエラーメッセージに露出                    |
| L4-6  | L   | `AttachmentLimits` / `DEFAULT_ATTACHMENT_LIMITS` が未使用の公開面 |

**裁定を台帳に書かないと、何巡回しても挙がり続ける**（蒸し返し防止が効かない）。

#### 台帳に ID が無いもの（修正中に出た。人間の裁定で ID を立てない運用）

| 内容                                                                                         | 記録先     |
| -------------------------------------------------------------------------------------------- | ---------- |
| `toUserIds` が**空でない応答に「応答が空です」**と言って起動ごと落ちる（`5055619` から在る） | `根F-*.md` |
| `shared/` と `mcp/` の **lint 境界が無い**（表は禁止を書いている。違反は現状ゼロ）           | `根E-*.md` |

### 手法ドラフトへの申し送り（未反映）

- **「自分の修正が偽にした記述」と「元からあった乖離」を分ける語彙**が要る。
  C-1 のとき前者を後者と誤って束ね、分ける判断をしてしまった（弱かった）
- **「根分けの接地」**が `指摘修正のあり方` に無い。統合と適用の境目にある穴
- **「直した側が台帳の状態列を触ってよいか」**の線引きがどこにも無い。
  スキルは「却下・削除・リナンバリングは人間権限」としか書いておらず、
  「修正済」への更新は名指しされていない。**統合者はここで踏み外した**（2026-09-07）
- **根の範囲と台帳の分類がずれることがある。** 根E は「DESIGN.md の乖離」として進めたが、
  台帳は L3-2・L3-9・L3-14（ツールの description）も根E に入れていた。
  **根に着手するとき、台帳でその根に分類された指摘を全部引く**手順が要る

---

## 4. 出所ごとの較正（**知らないと同じ罠を踏む**）

| 出所                       | 較正                                                                                                                                                                                                                                                                                                                                                      |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **L1**（正確性）           | probe による実体は本物。**行番号が系統的に誤り**（`audit.ts:364` → 実際 `:75`、39行のファイルに `:339`）。位置は必ず引き直す                                                                                                                                                                                                                              |
| **L2**（red-team）         | **主張は正しかったが、裏取りは成立していなかった** — 「ユーザー自身がハンドルネームを変更できる」は**2026-09-07 に一次情報で裏付いた**が、L2 が「確認済み」と書いた時点では読めていない先を挙げていた。**較正は変えない**（独立の裏取りが取れた主張だけ残す）。現にその手順が働いた。「7系統で防御成功」は依然として未再現＝未検証                        |
| **L3**（使う側）           | `tools/list` の現物と MCP 仕様のミラーで接地。問題なし                                                                                                                                                                                                                                                                                                    |
| **L4**（規約整合）         | `verify:rules` とカバレッジで接地。問題なし                                                                                                                                                                                                                                                                                                               |
| **実装者コメント**         | **行番号・テスト参照・引用は正確**（2巡とも全件一致）。本人が「記憶・裏づけ無し」と印を付けた部分は**根拠に使わない**。**自分の誤り（T-1 の `nonce` 共有案・「`pickName` の中で囲む」）を自分で撤回し、統合者の記述を2件正した**（2026-09-07）                                                                                                            |
| **統合者（前セッション）** | `git log -S` を**3回誤読**した。`-S` が答えるのは「文字列の増減」で、「挙動がいつ変わったか」ではない。**必ず差分を開く**                                                                                                                                                                                                                                 |
| **統合者（2026-09-07）**   | **段階2 を早く切り上げて推奨を2回覆した** — T-2 で `guardGateway` の先例（囲み）を読む前に JSON 化を推し、根D で「キーだけでは選べない」を人間に指摘されるまで名前を落としていた。**成果物の中の先例を探すのは段階2 の仕事**。加えて**送出の出所を誤って帰属した**（後検査を潰しても挙動が変わらなかった＝道具が答えていない問いに答えとして読んだ4度目） |

**一次情報は `docs/reference/` のミラー**（Backlog API 152エンドポイント＋MCP 仕様、`fetched: 2026-08-30`）。
`developer.nulab.com` は 200（2026-09-07 に5件をライブで照合し、ミラーと完全一致）。

**`support-ja.backlog.com` は読める（2026-09-07 に訂正）。** `curl` / `defuddle` / `WebFetch` /
`node` の `fetch` はすべて Cloudflare に **403** で弾かれるが、**`python3` の `urllib.request` は 200**。
**Zendesk の Help Center API**（`/api/v2/help_center/ja/articles/<id>.json`）なら本文と `updated_at` が
JSON で取れる。**「この環境から出られない」は道具の問題だった** — 前セッションも今回も
`curl` だけで判定していた。**403 を見たら道具を変えること。**

---

## 5. 約束事

### 作業の規律

- **巡は「レビュー巡」を数える。** 修正は巡の中の工程であって巡ではない。**回ったレビュー巡は第1巡だけ**で、
  済んだ5件は第1巡の適用（段階5）。次に再レビューを回せばそれが**第2巡**になる。
  2026-09-07 に台帳・`00-現在地.md`・`README.md` の表記を訂正した（人間の裁定）
- **`npm run check` が緑にならないうちは完了と報告しない**（`AGENTS.md`）。設定や依存を変えたら `npm run verify:rules` も
- **段階4（方針確定）で人間の確認を取る。** 確認前に段階5（修正）へ進まない
- **束ねない。** 隣接する別の指摘は、外した理由とともに方針に書く
- **根に着手するときは、台帳でその根に分類された指摘を全部引く。**
  根E は「DESIGN.md の乖離」として進めたが、台帳は L3-2・L3-9・L3-14 も根E に入れていた（取りこぼし）
- **台帳の状態列に「修正済」と書かない。** 解消の判定はレビュー側（第2巡）の仕事。
  却下・保留は**人間の裁定として**記録し、誰の裁定かを明記する
- **実装者セッションが並行して動いている。** 節目ごとに `git fetch` して取り込む
  （今日3回コメントが来た。行番号・引用は**全件正確**で、**順序制約を1件指摘してくれた**）
- **修正の直後に、自分が偽にした記述を洗う**（手順9）
- 作業の節目ごとに `obsidian/ai/logs/{date}.md` へ追記。書式は `## HH:mm — 見出し` ＋ `### 事実` / `### 解釈` / `### 決定`。**日時は暗算せず `Asia/Tokyo` で取得**

### スキルの発火

- 外部の仕様・API・バージョンに触れたら **`research-workflow`**（「知っている」と思ったときに適用）
- Obsidian へ書く前に **`obsidian-config`** → `obsidian-log`
- git 操作の前に **`git-workflow`**
- 節目のログは **`activity-log`**

### git

- コミットは**日本語・体言止め・prefix なし・句点なし・1行**。末尾に `Co-Authored-By`（リポジトリの先例に合わせる）
- **`git add .` を使わない**（`a/` `d/` を巻き込む）。明示パス指定
- **PR は作らない**（この作業では。トレインに乗せる判断が発生しないため）
- `.claude/skills/multi-angle-review/` は `.git/info/exclude` で除外済み

### 環境

- `node` は **v24.18.0**（nvm）。`Error.isError` あり。**実装者コメントにある `PATH=/opt/node24/bin` はこの環境には当てはまらない**
- `a/probe.png` `d/probe.png` は**以前の検証の残骸。放置と決めた**
- `.env` を読まない・実 Backlog へ通信しない

---

## 6. 次の一手（推奨）

**残り20件を直しきる。** 根はすべて閉じた（A・B・C・D・E・F ＋ T-1・T-2）が、
**単発と description 側が20件残っている**（§3）。

**第2巡（再レビュー）は、直しきってから回す。** 一度「再レビューが次」と推奨したが、
**未対応が20件ある状態で回しても「直っていない」と言われるだけ**である（人間の指摘で撤回した）。
とくに**保留7件は裁定が未記録**なので、何巡回しても挙がり続ける。

順序の提案:

1. **H 3件**（**L3-2**・**L3-3**・**L3-6**）— L3-3 と L3-6 は「schema の宣言と実挙動の食い違い」で隣接。
   L3-2 は根E の取りこぼしで、実物で確認済み
2. **M 7件・L 1件**の採否を決めて当てる（**L3-12** は MCP 仕様で裏取り済み）
3. **保留7件を台帳に裁定として記録する**（これをやらないと収束しない）
4. **そのうえで第2巡**

**第2巡を回すときの縮退手順**（手順4）: 各視点へ〈前巡で自分が出した指摘＋その解消状況〉を注入する
（他視点由来の判断は注入しない＝独立性の担保）。
**レンズ設計に反映すること**（台帳の較正4）: 一次情報に触れる視点には
①`research-workflow` の適用を明示 ②`docs/reference/` のミラーが正本であることを伝える
③「到達できなかった」も報告させる。**403 は道具の問題**（`python3` の `urllib` は通る）。

**未検証のまま残っていること**: 直した12件の**二次被害を独立に検証していない**。
ゲート・違反プローブ・自己点検はすべて統合者自身が回したもので、**レンズの目は入っていない**。

### 未解決 — テストが1回だけ落ちた（2026-09-07・再現せず）

`npm run check` で **1回だけ `fail 1`** が出た。その後 `node --test` を8回・`npm run check` を2回
回したが**再現しない**。**失敗したテスト名を取り逃がした**（grep で詳細を捨てた）ので特定できていない。

**2026-09-07 の追試では再現しなかった。** ただし `check` の exit 1 を4回見ており、
**すべて `prettier --check`** だった。**exit code だけで「テストが落ちた」と判断しない。**
`node --test` の要約は **`ℹ tests / ℹ pass / ℹ fail`**（TAP の `# fail` ではない）。
**ログは丸ごとファイルに落としてから grep する**（`| tail` に通すと件数行ごと消える）。

**握りつぶさずに残す。** 疑うなら根C で入れた `sonic-boom` 由来の非同期 —
`reopen` が成功したとき、**古い記述子を `'ready'` の nextTick で閉じる**。
テストスイートに新しく入った唯一の非同期がこれ。次に見かけたら**失敗名を必ず控えること**。

**2026-09-07（根F）の追試では再現しなかった** — `npm run check` を3回・`node --test` を単体で10回以上
回して `fail 0`。ただし**この日の `check` は2回 exit 1 を返しており、どちらも `prettier --check` だった**
（テストではない）。**exit code だけ見て「テストが落ちた」と判断しない。**

**件数行の書式に注意。** `node --test` の要約は **`ℹ tests / ℹ pass / ℹ fail`** で、TAP の `# fail` ではない。
`grep '^# fail'` は**常に空振りする**（前セッションが詳細を捨てたのと同じ形の罠）。
**ログは丸ごとファイルに落としてから grep する**（`| tail` に通すと件数行ごと消える）。

### 根C から持ち越した論点

- **`VERSIONS.md` の「実行時依存 — `got` のみ」は元から偽**（`@dotenvx/dotenvx` と `file-type` が既にある）。
  根C の修正で `sonic-boom` が加わって一段偽になったが、**元からあった乖離なので根E へ送る**。
  黙って直していない
- `withAudit.listTools` は素通しで何も記録しない（**根D を当てる段で決める**）
- `server.ts:160` の `writeAudit` が投げると元の起動失敗の例外が失われる（**起動時なので裁定が掛からない**）
- `stderrAuditSink` の `process.stderr.write` は EPIPE で投げうる（同上）
