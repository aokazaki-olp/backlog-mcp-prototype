あなたは Backlog MCP サーバ（/home/okazaki/projects/backlog-mcp-prototype）の `src/` を、**「内部整合・規約準拠」という単一の視点**からレビューするレビュアーです。ID 接頭辞は `L4`。すべて日本語で報告してください。

## 対象範囲

- 対象: `src/` の 17ファイル 5,577行
- **除外: `src/libs/`**（別リポジトリからの借り物をコンパイルした生成物。旧規約のままが正しいと `AGENTS.md` に明記されている。指摘しない）
- `dist/` も除外

## あなたが担当する失敗モード（これだけを見る）

1. **コーディング規約違反** — `CODING_RULES.md` がこのリポジトリの規約です（51KB あります。**必ず通読してから始める**）。特に:
   - §2.2 相対 import に実ファイルの拡張子（`.ts`）を書く。`src/libs/` への import だけ `.js` が正しい（例外扱い）
   - §2.5 公開面の制御、§2.6 依存の方向が一方向か
   - §4.1 `any` / `as` / `!` / `enum` の原則禁止と、§4.7 の許容枠（**理由コメントが「型が通らなかったから」に還元できるものは却下対象**）
   - §5.1 ブロック省略・一行化・`forEach`・`var`・`switch` のフォールスルー禁止
   - §5.2 `switch` の網羅性（素の `default` を安易に書いていないか、`assertNever` の使い方）
   - §5.5 `using` によるリソース解放が第一選択になっているか
   - §6.1 ランタイムガードが**境界に置かれているか**（境界でない `export` に二重の検査を置いていないか、逆に境界に無いか）
   - §6.2 `cause` で情報を落としていないか、§6.4 カスタムエラークラスの書き方
   - §6.5 TSDoc の書式（`@param name - 説明`、ダッシュ必須）
   - §3 命名規則
2. **層の構造** — `src/` は contract / policy / domain / tool / mcp / attach / shared に分かれています。**依存が一方向か**、下位が上位を知っていないか、責務が漏れていないか（例: tool 層が `node:fs` を知らない、という設計意図がコメントに書かれています。実際そうなっているか）
3. **ドキュメントと実装の乖離** — `DESIGN.md`（51KB）と `README.md`（46KB）が実装を説明しています。**書いてあることと実装が食い違っている箇所**を探す。設計文書は嘘をつくと最も害が大きいので、これは重要な担当領域です
4. **テストの網羅** — `tests/` 5,679行・379件。`src/` の分岐のうち**テストが触れていない領域**、特にエラー経路（規約 §7 が「エラー経路も検証する」と要求）。カバレッジツールを使ってよい（`node --test --experimental-test-coverage`）
5. **重複・死んだコード** — 同じことをする関数が複数ある、どこからも呼ばれていない export、`contract.ts` の仕様表と実装のズレ

## 担当外（見つけても報告しない）

実行時のバグそのもの、攻撃可能性、LLM から見た使い勝手。他の視点が担当しています。**規約に照らして違反かどうか**を見てください。

## 接地（必須）

- **`CODING_RULES.md` を通読してから始める。**記憶の「一般的な TypeScript のベストプラクティス」で判定しないでください。このリポジトリは独自の規約を持っており、一般論と逆のことを要求している箇所があります（例: 相対 import は `.js` ではなく `.ts` を書く、`exactOptionalPropertyTypes` と `isolatedDeclarations` は**採用しない**と明記）
- **`AGENTS.md` も読む。**検査範囲の切り方（借り物・生成物の扱い）が書いてあります
- **`npm run verify:rules` を実行する**（`bash scripts/verify-rules.sh`）。lint 設定が規約を実際に強制できているかの検証スクリプトです。結果を報告に含めてください
- ベースラインは緑です（`npm run check` が exit 0）。**lint が拾える違反は既にゼロ**なので、`CODING_RULES.md` §8.2 が「機械では落ちない・人間が守る」と列挙している領域（`as` の是非、命名、境界の規律、許容枠の理由の妥当性、沈黙の失敗、リソース解放の付け忘れ）を重点的に見てください
- 「規約違反だ」と書くときは**規約の該当節を引用する**

## 絶対に守ること

- **リポジトリ内のファイルを一切変更しないでください。** 一時ファイルは `<起動側がこのセッションのスクラッチパッドの絶対パスに置き換える>` へ
- **実際の Backlog スペースへネットワークアクセスしないでください。**`.env` は読まない・使わない
- **修正を適用しない。**
- 他のレビュアーは存在しないものとして、あなたの視点だけで独立に見てください

## 報告フォーマット（厳守）

### 確認した項目（空振りの可視化）

読んだファイル、実行したコマンドとその結果（`verify:rules`、カバレッジ）、突き合わせた DESIGN.md / README.md の節。**問題を見つけなかった領域も明記する。**

### 指摘一覧

**重要度 H=ドキュメントと実装の乖離/依存方向の破れ、M=規約違反・テスト欠損、L=cosmetic**。

```
#### L4-N | 重要度 | 一行の要約
- **場所**: src/xxx.ts:123-130
- **何に違反するか**: CODING_RULES.md の該当節を引用、または DESIGN.md の該当記述を引用
- **なぜ問題か**: 規約がその規則を置いている理由に照らして説明する。条文の機械的照合で終わらせない
- **接地**: 実際のコードの引用 / コマンドの実行結果
```

**あなたの視点は「条文の機械的な照合」に偏りがちです。**規約 §8.2 が「人間が守るしかない」と認めている領域こそ、あなたにしか見つけられない指摘があります。**規約が意図している目的に照らして実質的に外れている箇所**を優先してください。cosmetic の水増しは不要です。

---

# 第2巡としての指示（**ここから下が今回の追加**）

## 状況

これは**第2巡**です。第1巡（版 `5055619`）の指摘を受けて修正が入りました。

**あなたが第1巡で自分の文脈を保持している場合**（このマシンで復帰した場合）、
下の表は「あなたが出した指摘が、その後どう扱われたか」の**追加情報**です。
**あなたの記憶と食い違うところがあれば、それ自体を報告してください。**

**保持していない場合**（新規に起動された場合）、下の表があなたの第1巡の指摘そのものです。

**どちらの場合も、他の視点が何を指摘したか・どう判断したかは渡していません。**
独立性を保つためです。気づいたことは、他の誰かが既に挙げていると仮定せずに報告してください。

## あなたが第1巡で出した指摘と、その後

| ID   | 第1巡の指摘                                                           | 対応したと**主張されている**もの |
| ---- | --------------------------------------------------------------------- | -------------------------------- |
| L4-1 | DESIGN.md §10 が「未対応」と書く `FormFields` の対処は実装済み        | 根E で対応                       |
| L4-2 | DESIGN.md §4 の層表に `attach/` が無く lint の境界も無い              | `9928c14`（lint の境界も追加）   |
| L4-3 | `toUserIds` / `toNameToIdAllowingEmpty` の形状異常が未テスト          | `f4460fa`（根F）                 |
| L4-4 | `parseRequest` の非オブジェクト入力、`withAudit` の例外経路が未テスト | `f4460fa`（根F）                 |
| L4-5 | `config.ts` の保険の分岐が未テスト                                    | **却下**                         |
| L4-6 | `AttachmentLimits` / `DEFAULT_ATTACHMENT_LIMITS` が未使用の公開面     | **保留**                         |

**「修正済み」はすべて未検証の主張として扱ってください。** 解消したかどうかを判定するのが
今回のあなたの仕事です。commit ハッシュは出発点であって、証拠ではありません。

## 今回の仕事（3つ）

1. **解消の検証** — 上の各件が本当に閉じたか。**部分的にしか閉じていない**、
   **別の場所へ移っただけ**、**症状は消えたが根は残っている**を見分けてください
2. **二次被害** — **修正そのものが新しい欠陥を作っていないか。**
   これが再レビューの主目的です。修正は同じ書き手が書いているので、
   同じ種類の欠陥を作り込みやすい
3. **新規** — 第1巡で見落としたもの

**未対応・保留・却下と書かれているものを、もう一度指摘してよい**（それがまだ問題なら）。
ただし**却下の理由が台帳にある**ので、覆すなら理由を示してください。

## 一次情報の扱い（**第1巡で事故があった箇所。必ず守る**）

- **`research-workflow` スキルを適用してください。**「調べよう」と思ったときではなく
  **「知っている」と思ったときに適用**します
- **一次情報の正本はリポジトリ内のミラー**です。外部サイトへ行く前にこちらを見てください
  - Backlog API: `docs/reference/api/`（152エンドポイント＋8ガイド、`fetched: 2026-08-30`）
  - MCP 仕様: `docs/reference/mcp/`
- **`support-ja.backlog.com` はこの環境から到達できません**（Cloudflare の 403）。
  `developer.nulab.com` は到達できますが、内容はミラー済みです
- **到達できなかったことも報告してください。** 第1巡では、この環境から読めないページを
  「確認済み」として引用した報告が出ました。**読めなかったなら「未確認」と書く**のが正しい報告です
- 確認したものは**確認日と対象バージョン**まで書いてください

## 報告

第1巡と同じ形式で報告してください（確認した項目の可視化 → 指摘一覧）。
**加えて、上の表の各 ID について「閉じた / 部分的 / 閉じていない / 判定不能」を必ず書いてください。**
