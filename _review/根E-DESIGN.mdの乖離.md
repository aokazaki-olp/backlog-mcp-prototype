# 根E — DESIGN.md の記述と実装の乖離

**閉じる指摘**: L4-1、L4-2、および統合者が横断探索で追加した5件
**段階**: **5 完了**（2026-09-07）

---

## 段階1 — 理解

> **項目が済んで移動したあと、本文の参照が追随していない。**

DESIGN.md は冒頭で目的を宣言している。

> **捨てた案は、捨てた理由ごと残さないと必ず再提案される。**

§9 は「**次に触る人が最初に踏むもの**」と自称している。
**その文書が現状と食い違うと、次に読む人を能動的に誤らせる。**
コードのコメントが古いのとは害の質が違う — こちらは「これから何をするか」を決める文書。

---

## 段階2 — 調査（接地）

### 7件（すべて実物と突き合わせ済み）

| #   | 場所                | 記述                                                                                                        | 実際                                                                                     | 発見   |
| --- | ------------------- | ----------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------ |
| 1   | `DESIGN.md:443-447` | `FormFields` は `readonly FormValue[]`（スカラーの配列）を許す。対処は狭めること                            | `contract.ts:358` は `string \| number \| boolean \| AttachmentFile`。**既に狭めてある** | L4-1   |
| 2   | `DESIGN.md:152-160` | 層と語彙の表（7層）                                                                                         | **`attach/` が無い。** `eslint.config.js` にも境界が無い                                 | L4-2   |
| 3   | `DESIGN.md:220`     | 「全プロジェクトのマスタを起動時に引く」を**捨てた案**として掲載。「`can: "write"` を許したものだけに絞る」 | `server.ts:107` は `[...policy.scopes.keys()]`（**全許可プロジェクト**）を渡す           | 統合者 |
| 4   | `DESIGN.md:426`     | 「`can: "write"` を許したプロジェクトだけ〜引く」                                                           | 同上                                                                                     | 統合者 |
| 5   | `DESIGN.md:440`     | 「**この順にする**のは」                                                                                    | §10 の項目は「1. `preset`」の1つだけ。**順序が無い**                                     | 統合者 |
| 6   | `DESIGN.md:440`     | 「**1** が型システムの外側（ファイルの中身）を扱う唯一の層だから」                                          | 現在の 1 は `preset`。ファイルの中身を扱うのは添付で、**それは済んでいる**               | 統合者 |
| 7   | `DESIGN.md:445`     | 「**2** の `notifiedUserId[]` / `attachmentId[]` はまさにこれ」                                             | **2 が存在しない**                                                                       | 統合者 |

### 追加（2026-09-07・T-2 の調査で出たもの）

| #   | 場所           | 記述                                                                                             | 実際                                                                                                                                          | 発見   |
| --- | -------------- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 8   | `DESIGN.md` §6 | `unsafeOutputHint` / `secretHint` / `trustedHint` を**検討中**。「仕様に入ったら乗り換える対象」 | **入っていない**（`schema/2026-07-28/schema.ts` の `ToolAnnotations` は4つ）。**SEP-1561 と SEP-1487 は 2026-06-26 に dormant として closed** | 統合者 |

**外部の変化による乖離**で、こちらの修正が偽にしたものではない（申し送りの語彙の区別）。

### 3 と 4 がいちばん害が大きい

§5 は「**検討して捨てた案**」の表で、しかも「**再提案されやすいものから並べる**」と書いてある。
**実装が採用している形が、そこに「捨てた案」として載っている。**

次に読む人は、実装の挙動を見て「設計に反している」と判断し、**直しにいく**。
文書の目的（再提案を防ぐ）が、そのまま逆向きに作動する。

実装側には変更の理由が書いてある（`masters.ts:288-296`）。

> 以前は書き込みを許したプロジェクトだけに絞っていたが、**「状態で絞る」「担当者で絞る」は
> read の操作**なので前提が変わった。ここも並列なので、増えるのは同時実行数であって
> 直列の待ち時間ではない。

**理由は残っている。移されていないだけ。**

### 5・6・7 は §10 の番号が壊れている

§10 は「次段階と、その順序」。済んだ項目が引用ブロック（`>` 済み）へ移されていくが、
**本文の地の文が古い番号を参照したまま残っている。**

### #3・#4 の由来（**統合者が確認**）

実装者コメントの申告どおり **`fe51cb8`（子課題の件数と検索の絞り込みを到達可能にする）**で入った。
`git log -S` で `src/domain/masters.ts` の「状態で絞る」と `src/server.ts` の
「プロジェクト単位のマスタは許可プロジェクト全部」がともに `fe51cb8` を指すことを確認した（2026-09-06）。

> 統合者は最初 `-S 'policy.scopes.keys()'` で確かめて別のコミットを得ていた。**probe が誤り**で、
> その文字列は挙動の変更と対応していなかった。L1 の行番号・実装者の申告のどちらでもなく、
> **自分の道具の当て方が3度目の誤り**（00-現在地の考察2 と同型）。

### 段階3の対象に追加（実装者の自己申告）

実装者はこのレビュー版で DESIGN.md に**「添付のダウンロード」節と「添付の可否は `can` に足さない」節**を
足しており（`8f6ade0`）、**同種（済んだ項目の移動に地の文が追随していない）を新たに作っていないかは
確かめていない**と申告している。**段階3で DESIGN.md を通読するとき、この2節も対象に入れる。**

### 欠陥の種類（横断で探した対象）

> **済んだ項目が移動したあと、それを参照していた地の文が追随していない。**

この種類で DESIGN.md 全体を洗って7件。**L4 が挙げたのは2件だけ**だった。

**同種を README.md でも探すべきだが、未了**（46KB。段階3で行う）。
README は「何を・どう使うか」を書く文書なので、乖離があれば利用者に直接届く。

### 修正案の分類

**分類が件ごとに割れる。** 補遺は「指摘が正しいことは、成果物に置く理由にならない」と定めている。

| #       | 分類                   | 判定                                                                                                                                   |
| ------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 1       | **消す**               | 対処が済んでいるので、記述ごと落とす。§10 は「次段階」の節であって完了記録の置き場ではない                                             |
| 2       | **書くべき**           | 表に層が1つ欠けている。DESIGN.md §4 自身が「一部だけ機械強制側へ持ってきている」と書く以上、**その「一部」の範囲を示す責務は表が負う** |
| 3・4    | **書くべき（狭める）** | 捨てた案の表から外し、採用した形と理由を移す。**「消す」ではなく「範囲の変更」なので、足す修正と同じ波及の確認が要る**（補遺の実例3）  |
| 5・6・7 | **消す**               | 参照先が無い地の文。番号を振り直すのではなく、**参照そのものを落とす**方向が筋（項目が1つしか無いので「順序」の概念が要らない）        |

---

## 段階3 — 整合性の確認（**完了・2026-09-07**）

### 表と実装とlintを突き合わせた（`src/` の全モジュールを対応付け）

`src/` の import を機械的に取り出して、DESIGN.md §4 の表7行と `eslint.config.js` の境界3つに当てた。

| モジュール      | 表の行   | lint の境界 | 実際の依存                                                                             |
| --------------- | -------- | ----------- | -------------------------------------------------------------------------------------- |
| `contract.ts`   | ある     | —           | **import ゼロの葉**（表のとおり）                                                      |
| `shared/`       | ある     | **無い**    | 3本とも import ゼロ（表のとおり）                                                      |
| `policy/`       | ある     | ある        | `contract` / `shared` / `node:crypto`                                                  |
| `domain/`       | ある     | ある        | `contract` / `shared` / `libs` / `gateway`                                             |
| `tool/`         | ある     | ある        | `contract` / `shared` / `domain` / `policy` / `mcp/protocol` / **`attach`（型のみ）**  |
| `mcp/`          | ある     | **無い**    | `contract`（`ConfigError` だけ）/ `shared` / `node:fs` / `sonic-boom`                  |
| `libs/`         | ある     | —           | 借り物                                                                                 |
| **`attach/`**   | **無い** | **無い**    | `contract` / `shared` / `file-type` / `node:crypto` / `node:fs/promises` / `node:path` |
| **`config.ts`** | **無い** | 無い        | `contract` / `shared` / **`attach`（値）** / `@dotenvx/dotenvx` / `node:path`          |
| **`server.ts`** | **無い** | 無い        | ほぼ全部（**組み立て役**）                                                             |
| **`main.ts`**   | **無い** | 無い        | `server` / `mcp/stdio` / `shared`                                                      |

**欠落は `attach/` だけではなかった**（ノートの段階2 の見立てどおり）。
そして**表は7行あるのに lint は3つしかない** — `shared/` と `mcp/` は表が禁止を書いているのに機械が見ていない。

### 段階3 で出た新しい乖離（**grep では出なかった**）

**`masters.ts:306` の TSDoc が偽。**

```
 * **書き込みを許したプロジェクトでしか呼ばない。** read しか無いプロジェクトのぶんまで
 * 引くと、使わない呼び出しが起動時に並ぶ。
```

同じファイルの `343` 行は「**以前は書き込みを許したプロジェクトだけに絞っていたが…前提が変わった**」と
正しく書いている。**同一ファイル内で矛盾している。** #3・#4 と同じ由来（`fe51cb8`）。

補遺の実例6 が言うとおり、**依存は文字列として一致しない** — 「全プロジェクト」で検索しても出ず、
近傍を読んで初めて出た。

### README の横断（ノートが「未了」としていたもの）

**README:291 は正しい**（「許可プロジェクト全部について引く…以前は書き込みを許したプロジェクトだけに
絞っていた」）。**README だけが追随していた** — DESIGN.md と `masters.ts` の TSDoc が取り残された形。

新たに2件出た。

| #      | 場所                 | 記述                                                                                     | 実際                                                                                                                                                      |
| ------ | -------------------- | ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **R1** | `README.md:287`      | 選べる名前は「**どれも Backlog の管理者が付けた名前で、第三者の自由記述ではない**」      | **偽**（根B で分類をやり直した）。しかも**同じ README の 460 行以降の表と矛盾**している（そこでは課題種別・カテゴリー等を「第三者が書ける」と書いている） |
| **R2** | `README.md` 囲みの表 | 添付ファイル名・Wiki ページ名・`list_project_masters` は「トレードオフ。**裁定は保留**」 | **2026-09-07 に裁定済み**（T-2 ①②③）。**自分の作業が偽にしたもの**（手順9）                                                                               |

**R1 は C-2 と同型**（`lookupName` の TSDoc が同じ断言をしていた）。**TSDoc は根B で直したが README は直していない。**

### #1 の「消す」— 前提は残る（確認済み）

`DESIGN.md:443-447` が運んでいる「借り物の `ApiClient` がスカラー配列を弾く」という事実は、
**`contract.ts:352-357` の TSDoc に同じ内容が書かれている**（実物を開いて確認）。
`FormValue` は既に `string | number | boolean | AttachmentFile` に狭めてあり、
**「次段階で上流を直すか回避するかを決める」という結論もそちらに残っている**。→ **落としても前提は失われない。**

### 書き出すべきもの（未着手）

**#2（`attach/` の追加）は「足す」修正で、危険度が最も高い。**
表に行を1つ足すと、`attach/` の「知ってよい／知ってはいけない」を**決めなければならない**。
現状を書き写すのではなく、**設計として何が正しいか**を決める必要がある。

- `attach/localFile.ts` は `node:fs/promises` / `node:path` / `node:crypto` / `file-type` を使う
- `tool/tools.ts` は `attach/localFile.ts` から**型だけ** import している（`ReceivedAttachment`）
- `config.ts` は `attach/localFile.ts` から**値** を import している（`isInside` / `resolveAttachmentRoot`）

**`config.ts` → `attach/` の依存は、表のどの層にも属さない。** `config.ts` は表に無い。
つまり**表の欠落は `attach/` だけではない可能性がある** — 段階3で `src/` の全モジュールが
表のどの行に当たるかを対応付ける必要がある（`main.ts` / `server.ts` / `config.ts` も未掲載）。

**#3・#4 は「狭める」修正。** §5 の表から1行を外すと、
**その行が暗黙に与えていた前提**が無いかを確かめる必要がある（補遺の実例3「狭めすぎ」）。
候補: 「read しか無いプロジェクトのぶんまで引くと、使わない呼び出しが起動時に並ぶ」という
**コストの議論**が他所で参照されていないか。

**#1 は「消す」修正だが、例が前提を担っていないか確かめる。**
`DESIGN.md:443-447` は `FormFields` の話をしながら、
**借り物の `ApiClient.splitFormFields` がスカラー配列を弾く**という事実を運んでいる。
この事実は `contract.ts:352-357` と `tools.ts:1121-1124` にも書かれているので**重複だが**、
「複数添付をやるときに上流の修正が要る」という結論の根拠になっている。
§10 の引用ブロック（`DESIGN.md:430-434`）が既に同じことを書いているので、
**落としても前提は残る** — ただし段階3で確認する。

### 段階3を検索で済ませない

補遺の実例6が挙げているのはまさにこの形。
**依存は文字列として一致しない。** DESIGN.md・README.md・コードのコメントは
同じ事実を別の語で書いているので、`grep` では出てこない。
**変更する箇所の近傍を通しで読む。**

---

## 段階4 — 方針確定（未着手）

段階3の出力が出てから。ここで人間の確認を取る。

**先に決めておくべきこと**: #2 で `attach/` に与える語彙（＝`attach/` は何を知ってよいか）。
これは記述の修正ではなく**設計の決定**なので、人間の判断領域。

---

## 段階4 — 修正方針（**確認待ち**・2026-09-07）

### 裁定済み（人間）

| #        | 裁定                                                                                             |
| -------- | ------------------------------------------------------------------------------------------------ |
| #2 の形  | **`attach/` の行 ＋「組み立て役」行**を足す（表の欠落を全部埋める）                              |
| 置き場所 | **`isInside` / `resolveAttachmentRoot` を `shared/` へ移す**（`config.ts → attach/` の辺を消す） |
| lint     | **`attach/` だけ**境界を足す                                                                     |

### 順序が決まっている（**実装者コメントの指摘**）

**#2 → #6 の順で当てる。** `DESIGN.md:440` の

> **この順にするのは、1 が型システムの外側（ファイルの中身）を扱う唯一の層だから。**

は #6 で「消す」分類だが、**この一文が `attach/` の語彙そのもの**である。
**先に消すと #2 の錨が失われる**（補遺の実例3「例が暗黙に前提を担っていた」）。
統合者の段階3 はこの依存を見ていなかった。

### 当てるもの

| 順  | 対象                       | 操作       | 中身                                                                                                                                                                                                                |
| --- | -------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `shared/` にパス計算を移す | **移動**   | `isInside` / `resolveAttachmentRoot`（どちらも `node:path` だけの純関数。実物で確認）。`shared/` の行の記述を広げる                                                                                                 |
| 2   | **#2** DESIGN.md §4 の表   | **足す**   | `attach/`（型システムの外側・パス・マジックバイト ／ Backlog・MCP・ポリシー・HTTP を知らない）＋ `config.ts` / `server.ts`（**全層を知る唯一の組み立て点**）/ `main.ts`。**`tool/ → mcp/`（ハンドラ型）も行に書く** |
| 3   | lint                       | **足す**   | `src/attach/**` から `mcp/` `tool/` `policy/` `domain/` `libs/` を禁止。`verify:rules` に違反プローブを1件足す（規約 §8.3）                                                                                         |
| 4   | **#6**                     | **消す**   | 参照先の無くなった地の文（**2 の後**）                                                                                                                                                                              |
| 5   | **#1・#5・#7**             | **消す**   | 済んだ項目への参照。#1 の前提は `contract.ts:352-357` に残ることを確認済み                                                                                                                                          |
| 6   | **#3・#4**                 | **狭める** | §5「捨てた案」から1行を外し、採用した形と理由を移す                                                                                                                                                                 |
| 7   | **#8**                     | **直す**   | SEP-1561 / SEP-1487 は dormant で closed。「仕様に入ったら乗り換える」を現状に合わせる                                                                                                                              |
| 8   | `masters.ts:306` の TSDoc  | **直す**   | 段階3 で見つけた新規。同一ファイル内で 343 行と矛盾                                                                                                                                                                 |
| 9   | **R1** `README.md:287`     | **直す**   | 「どれも管理者が付けた名前」は偽。**同じ README の囲みの表と矛盾**                                                                                                                                                  |
| 10  | **R2** README 囲みの表     | **直す**   | 「裁定は保留」→ T-2 で裁定済み。**自分が今日偽にしたもの**（手順9）                                                                                                                                                 |

### 変更しないと決めた箇所（名指しで外す）

- **`shared/` と `mcp/` の lint 境界** — 表が禁止を書いているのに機械が見ていないが、**違反は現状ゼロ**。
  L4-2 の範囲外。**台帳へ新規として残さず、この修正側の記録に残す**（人間の裁定・2026-09-07 の運用）
- **表そのものをやめる案**（規約 §1.3「列挙は必ず実際の集合から遅れる」）— 設計の大きな変更。別の根

### 検証の設計

| 種類             | 内容                                                                                            |
| ---------------- | ----------------------------------------------------------------------------------------------- |
| 赤になるはず     | `attach/` から `domain/` を import するコードが lint で落ちる（`verify:rules` のプローブ）      |
| 緑のまま（境界） | 既存の `attach/` の import（`contract` / `shared` / `file-type` / `node:*`）は通る              |
| 移動の検証       | `npm run check` 緑。**既存テストが動くかは移動なので動きうる** — 動いたら import の付け替え漏れ |

---

## 段階5 — 修正（2026-09-07・完了）

**順序を守った**（#2 → #6）。実装者コメントの指摘どおり、先に語彙を移してから参照を落とした。

| 順  | 当てたもの                                                                                                                                                                                                         | 結果                                                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------- |
| 1   | `src/shared/paths.ts` を新設し、`isInside` / `resolveAttachmentRoot` を移動                                                                                                                                        | **`config.ts → attach/` の辺が消えた**                   |
| 2   | **#2** DESIGN.md §4 の表に `attach/` を追加。`shared/` の記述を「言語機能と純粋な計算」へ広げ、**組み立て役（`server.ts` / `main.ts` / `config.ts`）は層として数えない**ことと **`tool/ → mcp/`（型のみ）** を明記 | 表の欠落が全部埋まった                                   |
| 3   | `eslint.config.js` に `src/attach/**` の境界。`verify:rules` に違反プローブを1件追加                                                                                                                               | **29/29 成功**（28 → 29）                                |
| 4   | **#6** 「1 が型システムの外側を扱う唯一の層だから」を削除                                                                                                                                                          | 語彙は §4 へ移した後                                     |
| 5   | **#1・#5・#7** §10 の地の文（`FormFields` の段落・番号参照）を削除                                                                                                                                                 | 前提は `contract.ts:352-357` に残る（確認済み）          |
| 6   | **#3・#4** §5 の「捨てた案」から1行を外し、§10 の引用ブロックに採用形と理由を移した                                                                                                                                | 実装と一致                                               |
| 7   | **#8** §6 の SEP の状況を現状（dormant で closed）に更新                                                                                                                                                           | —                                                        |
| 8   | `masters.ts:306` の TSDoc                                                                                                                                                                                          | 段階3 で見つけた新規。343 行との矛盾が解消               |
| 9   | **R1** `README.md:287`                                                                                                                                                                                             | 「どれも管理者が付けた名前」を削除し、囲みの表へ誘導     |
| 10  | **R2** README 囲みの表                                                                                                                                                                                             | 「裁定は保留」→ 裁定済み。**エラー経路は囲む**ことも明記 |

**ゲート**: `npm run check` 緑・**436件 pass**（変化なし）／`verify:rules` **29/29**。

**テスト件数が変わらなかったことが、移動が正しく行われた証拠になる**
（関数を移しただけで挙動は変えていない。import の付け替え漏れがあれば型チェックで落ちる）。

### 違反プローブ（手順7）

`verify:rules` に足した1件がそれに当たる。

```
attach 層から domain 層への import を禁止する   → 落ちる（29/29 成功）
```

**表に行を足しただけでは「宣言」にしかならない**（実装者コメントの指摘）。機械が見る形にした。

### 自己点検（手順9）

- **`shared/` の記述を広げた**のは「広げる」修正で、危険度は「足す」に近い（補遺の実例3）。
  広げた先は**パス計算だけ**で、`I/O` は「知ってはいけない」側に明記した。
  `assertNever` にランタイム依存を入れない制約は規約 §5.2 が別に持っているので、二重に書いていない
- **`localFile.ts` から `resolve` の import が未使用になった**のを lint が捕まえた。移動のたびに起きる形
- **R2 は自分が今日偽にした記述**（T-2 の裁定）。**手順9 が2回連続で効いた**

### 束ねなかったもの

- **`shared/` と `mcp/` の lint 境界** — 表が禁止を書いているのに機械が見ていない。**違反は現状ゼロ**。
  L4-2 の範囲外なので、**この記録に残して台帳へは新規 ID を立てない**（人間の裁定・2026-09-07 の運用）
- **表そのものをやめる案**（規約 §1.3「列挙は必ず実際の集合から遅れる」）— 設計の大きな変更。別の根
