# 根B — ユーザーの表示名を「管理者が定義した値」に分類している

**閉じる指摘**: L2-3、C-2（統合者が横断探索で追加）、**L1-4（→ 修正済み。下記）**
**段階**: **5 完了（2026-09-07）**。**L1-4 も 5 完了**。**C-2 は T-2 へ移した**
**T-1 は裁定済み**（`L3-5-囲みのコストと書式.md`。単価は 254 → 107 バイトに下がった）

---

## 段階1 — 理解

指摘者の言葉から離すと、症状はこうなる。

> **Backlog のユーザーは自分の表示名を自分で決められるのに、
> このサーバは表示名を「管理者が定義した閉じた選択肢」として扱っている。**

その結果が2通りに現れる。

- 表示名が `<untrusted>` で囲まれずに LLM へ届く（同じ応答の `summary` は囲まれているのに）
- 表示名とログイン名を1つの索引で混ぜているため、**衝突すると別人に解決される**

---

## 段階2 — 調査（接地）

### 判断軸は存在し、一貫して適用されている

**最初の見立て（「囲む対象が決まっていない」）は誤りだった。**
コードベースは**「第三者が書けるか」という軸を明示し、ほぼ全箇所で正しく適用している**。

| 場所              | コード中の言葉                                                                                                                           |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `tools.ts:675`    | 「件名も第三者が書ける。一覧では本文より先に読まれるので、囲まないと素通しになる」                                                       |
| `tools.ts:839`    | 「説明は第三者が書ける」                                                                                                                 |
| `tools.ts:496`    | 「数値2つなので囲まない（第三者が書けるテキストではない）」                                                                              |
| `tools.ts:547`    | 「未知の `fieldTypeId` で文字列が来たら**囲む側へ倒す** — 第三者が書けるかどうか分からないものを素通しするより、余分に囲むほうが安全側」 |
| `untrusted.ts:38` | `UntrustedSource.name` の型注釈が「**第三者が書ける名前**」と軸そのものを宣言している                                                    |

DESIGN.md §8 も同じ軸で書かれている（`DESIGN.md:287-299`）。

**欠陥は軸の不在ではなく、1つの値の誤分類。** これは修正の当て方を変える —
軸を作り直すのではなく、**誤分類された値を正しい側へ移す**話になる。

### 誤分類が現れる3箇所

| #   | 場所                                   | 現れ方                                                                                                                                                                                                            | 発見                   |
| --- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- |
| 1   | `tools.ts:467` `pickName`              | `assignee` / `createdUser` / `updatedUser` が無囲い。`shapeIssue` / `shapeComment` / `shapeWikiPage` / `shapePullRequest` / `shapeActivity` / `shapeDocument` / `shapeAttachment` の**全部**から呼ばれる          | L2-3                   |
| 2   | `tools.ts:1675` `list_project_masters` | `assignees`（`members`）と `ambiguousUserNames` が無囲い                                                                                                                                                          | 部分的に既知           |
| 3   | `masters.ts:363-388` `lookupName`      | **TSDoc が「どれも Backlog の管理者が付けた名前（第三者の自由記述ではない）」と断言。** `resolveAssignee` 経由で `project.userIds` に対して呼ばれ、失敗時に**最大20人ぶんの表示名・ログイン名を無囲いで**出力する | **統合者（横断探索）** |

3番目は**どのレンズも挙げていない**。L2-3 の欠陥の種類を
「ユーザーの表示名を管理者定義側に分類している」と言葉にして `src/` 全体を洗った結果。

### L2-3 の引用元は誤り（統合時に訂正済み）

L2-3 は `pickName` に「選択肢は管理者が定義したもの」というコメントがあると書いたが、
実際にその文があるのは `tools.ts:542`（**カスタム属性の `{id,name}` について**）である。

> **【2026-09-07 訂正】** ここに「**そちらは正しい（カスタム属性の選択肢は管理者が定義する）**」と
> 書いていたが、**誤り。** ミラーによると**リスト項目の追加は「すべての権限」**
> （更新・削除だけが「管理者 / プロジェクト管理者」）。誰でも任意の選択肢を足せる。
> 段階3 の表を見ること。

`pickName` の TSDoc が述べているのは**フィールドの漏洩防止**である。

> **ユーザーオブジェクトを出力に載せる唯一の経路。** Backlog のユーザーは
> `id` / `userId` / `name` / `roleType` / `lang` / `nulabAccount` / `mailAddress` /
> `lastLoginTime` を持ち〔…〕**ここを通す限り `name` しか出ない**

つまり `pickName` が守っているのは「`mailAddress` を出さない」ことであって、
**信頼分類は検討された形跡が無い。** 指摘としてはむしろ強まる。

### L1-4（別人に解決される）の機構

`toUserIds`（`masters.ts:140-175`）は **1つの `result` Map を2つの名前空間で共有している。**

```
ループ1:  result.set(loginName, id)     ← ログイン名の空間
ループ2:  for ([name, id] of byName)
            if (!ambiguous.has(name))
              result.set(name, id)      ← 表示名の空間。ログイン名の項目を上書きしうる
```

`ambiguous` が守っているのは **`byName`（表示名どうしの衝突）だけ**。
**表示名 × ログイン名の衝突は一切検査していない。**
ユーザーA（`userId: "sato"`）とユーザーB（`name: "sato"`）がいると、
`assignee: "sato"` は**Bに解決される**。`ambiguousUserNames` は空のままなので警告も出ない。

`toUserIds` の TSDoc は「**黙って先勝ちにしない**（規約 §5.4 — 別人に割り当てるのは静かな失敗）」
と明言している。**その当の失敗が、別経路で起きている。**

### いつ入ったか（`git log -S` と差分で確認）

`ambiguousUserNames` と2重ループの構造は **`3957192`（課題の作成と更新を追加）**で入っている。
**後から壊れたものではなく、最初からの誤り。**（根A とは形が違う。）

`toUserIds` はその後3回編集され、DESIGN.md §9 はそのうち2回を実データ事故として記録している
（人数が二重に見える → その修正が `userId: null` を落とした）。
**同じ関数を3回触り、2回の事故を記録しながら、L1-4 は3回とも素通りした。**

> `git log -S "result.set(loginName"` は `a445d56` を返すが、**差分を見ると
> この commit は既存の `result.set(item['userId'], ...)` を変数に括り出しただけ**で、
> 構造の導入ではない。`-S` が拾うのは文字列の増減であって構造ではない。

### 一次情報の裏取り — **L2 の裏取りは成立していない（2026-09-06 訂正）**

L2 は「Backlog ヘルプセンター（`support-ja.backlog.com`）の2記事で、
**ユーザー自身がハンドルネームを変更できる**ことを確認済み」と報告した。**これは検証できない。**

| 確かめたこと                                                             | 結果                                                                                                                                  |
| ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------- |
| `support-ja.backlog.com` への到達（curl / ブラウザ UA の2通り）          | **どちらも 403。** Cloudflare の「Just a moment...」チャレンジ                                                                        |
| `developer.nulab.com` への到達                                           | 200（到達可能。ただし内容はミラー済み）                                                                                               |
| DESIGN.md:332 の記述                                                     | 「**API ドキュメントではなくサポートサイトの話**（`support-ja.backlog.com`）。**この環境から出られない**」                            |
| ミラー（`docs/reference/api/`、152エンドポイント）に表示名の記述があるか | **無い。** `update-user` エンドポイントが存在せず、`name` フィールドの説明も無く、「表示名 / ハンドルネーム / ニックネーム」の語も0件 |

**したがって「ユーザー自身が表示名を変更できる」は未確認とする。** L2 の引用は、
この環境から読めない先を根拠として挙げている。

> **記録**（`research-workflow` の「記録の仕方」に従う）
>
> - 確認日: 2026-09-06
> - 到達性: `https://support-ja.backlog.com/hc/ja/articles/360035641834` → **403**（curl / ブラウザ UA の2通り、`--max-time 15`）。本文は Cloudflare の `<title>Just a moment...</title>`
> - ミラー: `docs/reference/api/`（`source: developer.nulab.com`、**`fetched: 2026-08-30`**、152エンドポイント）
> - 未確認の内容: **ユーザーの表示名（`name`）を誰が設定・変更できるか**
> - 3方向の確認: ①実際の挙動（403）②手元の実物（DESIGN.md:332 の記載）③公式文書のミラー（記述なし）

### 未確認になっても根Bが残る理由（2つとも接地済み）

**1. ミラーに現れる非対称。** 他のマスタには**管理者が定義する API がある**が、
ユーザーの表示名には無い。

| 値                   | 定義する API（ミラーに実在）                                                           |
| -------------------- | -------------------------------------------------------------------------------------- |
| `status`             | `update-status.md` / `update-order-of-status.md`                                       |
| `category`           | `update-category.md`                                                                   |
| `issueType`          | `update-issue-type.md`                                                                 |
| `milestone`          | `update-version-milestone.md`                                                          |
| **ユーザーの表示名** | **無い。** `get-project-user-list.md` の応答例では `nulabAccount` 由来の値として現れる |

つまり**「空間管理者が定義した閉じた選択肢」という分類は、少なくともユーザーには当てはまらない。**
誰が書けるかは未確認でも、**他のマスタと同じ扱いにする根拠は無い。**

> **【2026-09-07 訂正】この論法は弱い。** `priority` / `resolution` にも**書き込み API は無い**
> （`GET /api/v2/priorities` / `/resolutions` のみ）ので、**API の有無だけでは分類の根拠にならない**。
> しかも `issueType` / `category` / マイルストーンは API があるが**権限は「すべての権限」**で、
> 管理者定義ではなかった。**残るのは下の2番（コード自身の方針）と、応答例で
> `user.name` と `nulabAccount.name` が一致すること。** 段階3 を見ること。

**2. コードベース自身の方針が「分からないなら囲む」。** `tools.ts:547`:

> 未知の `fieldTypeId` で文字列が来たら**囲む側へ倒す** — 第三者が書けるかどうか分からない
> ものを素通しするより、**余分に囲むほうが安全側**。

**未確認であることは、この方針では囲む理由になる。** 外部の事実に依存せず、
成果物自身の原則から同じ結論が出る。

> **段階3で扱う**: 「本人が変更できる」を**根拠として書かない**。書くなら
> 上の2つ（ミラーの非対称・コード自身の方針）を根拠にする。
> 未確認の事実を方針の根拠に据えると、補遺の「誤りを、測っていない別の主張に置き換える」に当たる。

### 修正案の分類

| 分類         | 判定                                                              |
| ------------ | ----------------------------------------------------------------- |
| **書くべき** | 3箇所とも該当。誤分類が残る限り、囲みの緩和は部分的にしか効かない |

---

## 実装者コメントからの入力（統合者が実物で検証済み）

### `toUserIds` の各行が保証しているもの（コード由来）

素朴に直すと**過去2回の事故を踏み直す**ので、何を守っているかを分解しておく。

| 行                   | 何を保証しているか                                                   | 壊すと起きること           |
| -------------------- | -------------------------------------------------------------------- | -------------------------- |
| `masters.ts:157-160` | `userId` が空文字・`null` のユーザーを**ログイン名の索引に入れない** | `null` がキーになる        |
| `masters.ts:161-163` | ログイン名は一意なので**必ず指せる**                                 | —                          |
| `masters.ts:166-168` | `members` に**1人につき1件**（ログイン名の有無にかかわらず）         | **DESIGN.md §9 の事故2つ** |
| `masters.ts:171-175` | 表示名は**曖昧でなければ**指せる                                     | 別人に割り当てる（§5.4）   |

**欠けているのは「表示名 × ログイン名」の衝突検査だけ。**

### 素朴な修正は既存テストで止まる（**統合者が4件とも実物で確認、2026-09-06**）

| テスト                          | 何を固定しているか                                              | 確認          |
| ------------------------------- | --------------------------------------------------------------- | ------------- |
| `tests/masters.test.ts:247`     | `userId: null` のユーザーも1件として並ぶ                        | ✅            |
| `tests/masters.test.ts:275-277` | 表示名とログイン名の**両方**で引ける・`ambiguousUserNames` が空 | ✅ 記載どおり |
| `tests/masters.test.ts:280-297` | 同名なら**表示名では引けなくなり**、ログイン名では引ける        | ✅ 記載どおり |
| `tests/tools.test.ts:1767`      | `assignees` が**1人1件**                                        | ✅            |

**「表示名を索引から外す」は通らない。** `userId: null` のユーザーは表示名でしか指せず、
それを落としたのが DESIGN.md §9 の2回目の事故（`a445d56` で直したもの）。

> 実装者コメントの行番号は**4件とも正確だった**。L1 の系統的なずれとは対照的で、
> この出所は位置情報も信用してよい（ただし個別には確かめる）。

### 修正の選択肢（実装者コメント。**どれも未検証**）

1. **衝突した表示名を `ambiguous` に入れる** — 既存の「曖昧なら表示名で引けない」に合流する
2. ログイン名の索引を優先し、衝突した表示名だけ落とす
3. 索引を2つに分け、`resolveAssignee` がログイン名 → 表示名の順に引く

**1 が既存の錨に一番近い**（補遺「文書内の錨を先に探す。新概念の導入は最後の手段」）。
ただし `ambiguousUserNames` の意味が「表示名が重複」から「表示名で指せない」に広がるので、
**`list_project_masters` の note の文言が寄りかかっていないかを確かめること。**

### 段階3の問いが1つ解けた — `pickName` は中で囲む（**統合者が確認**）

「`pickName` を分割したり呼び出し側で囲むと『唯一の経路』が崩れる恐れ」と書いていた点に答えが出た。

**`pickName` の中で囲むほうが、フィールド漏洩の要件を落とさない。**
`tests/tools.test.ts:735` の `PII_PATTERN`
（`/userId|roleType|nulabId|nulabAccount|mailAddress|lastLoginTime|uniqueId/`）が
この性質を固定していることを実物で確認した。`tools.test.ts:473` にも同型の検査がある。

### T-1 について（実装者の見立て・**未検証**）

囲みのコストを下げるなら、**`nonce` を呼び出しごとの乱数にしている理由**が制約になる
（`untrusted.ts:93-95`「固定文字列だと本文側に書かれた閉じタグで抜けられる」）。
**1応答の中で `nonce` を共有すれば**定型文の繰り返しを1回に減らせるはずだが、
「同じ `nonce` の囲みが複数あると、間の地の文が囲みの中に見えるか」は**測っていない。**

---

## 段階3 — 一次情報で分類をやり直した（2026-09-07）

**ミラーを引いたら、誤分類は1つの値ではなく分類そのものだった。**
（`docs/reference/api/`、152エンドポイント、`fetched: 2026-08-30`。統合者が自分で引いた）

### 誰が書けるか（ミラーの「実行可能な権限」を原文どおり）

| 値                           | 追加               | 更新              | 削除              | 判定                           |
| ---------------------------- | ------------------ | ----------------- | ----------------- | ------------------------------ |
| `priority`                   | **口が無い**       | 無い              | 無い              | **固定**（スペース全体）       |
| `resolution`                 | **口が無い**       | 無い              | 無い              | **固定**                       |
| `status`                     | 管理者             | 管理者            | 管理者            | **管理者定義**                 |
| **`issueType`**              | すべての権限       | すべての権限      | すべての権限      | **第三者が書ける**             |
| **`category`**               | すべての権限       | すべての権限      | すべての権限      | **第三者が書ける**             |
| **`milestone` / `versions`** | すべての権限       | すべての権限      | すべての権限      | **第三者が書ける**             |
| **ドキュメントの `tags`**    | すべての権限       | —                 | すべての権限      | **第三者が書ける**             |
| Wiki の `tags`               | **API に口が無い** | —                 | —                 | 不明（管理者定義の根拠も無い） |
| カスタム属性の**定義**       | 管理者 / PJ管理者  | 管理者 / PJ管理者 | 管理者 / PJ管理者 | 管理者定義                     |
| **リスト項目**               | **すべての権限**   | 管理者 / PJ管理者 | 管理者 / PJ管理者 | **第三者が追加できる**         |
| ユーザーの `name`            | **口が無い**       | 無い              | 無い              | **未確認**（後述）             |

**比較の基準**: `add-comment`（第三者が書く典型）も**「すべての権限」**。
ミラーは `管理者` と `すべての権限` を**書き分けている**（`update-status` は `管理者`）ので、
この対比は成立する。ただし「すべての権限」が指す役割の集合は、ミラーの guides に定義が無い（未確認）。

### ライブの公式サイトでも確認した（3方向・2026-09-07）

ミラーは `fetched: 2026-08-30` なので、**現行版と食い違っていないか**を `developer.nulab.com` で直接確かめた。

| エンドポイント                             | ミラー                 | ライブ（2026-09-07） |
| ------------------------------------------ | ---------------------- | -------------------- |
| `update-status`                            | 管理者                 | **管理者**（一致）   |
| `update-category`                          | すべての権限・制限なし | **一致**             |
| `update-issue-type`                        | すべての権限・制限なし | **一致**             |
| `add-document-tag`                         | すべての権限・制限なし | **一致**             |
| `add-list-item-for-list-type-custom-field` | すべての権限・制限なし | **一致**             |

**3方向**: ①ミラー（手元の実物）②ライブの公式ドキュメント ③コードの実装（どう扱っているか）。

### ユーザーの表示名 — **確定した（2026-09-07・一次情報を読んだ）**

**Backlog ヘルプセンターの「自分のユーザー情報を変更する」を読めた。**

> - 出典: `https://support-ja.backlog.com/hc/ja/articles/360035641834`
> - **記事の更新日: 2026-09-01**（Zendesk の `updated_at`。現行版）
> - 取得日: 2026-09-07
> - 取得方法: **`python3` の `urllib`**（下記）

原文のまま引用する。

> **ハンドルネーム、アイコン**
> アカウント情報として、ヌーラボアカウントの情報を使用するか、Backlogの情報を使うかを選択できます。
>
> **ヌーラボアカウントの情報を使う**
> ハンドルネームやアイコンは 「ヌーラボアカウントを編集」の「アカウント」から変更するとBacklogにも反映されます。
>
> **Backlogの情報を使う**
> ユーザーのハンドルネームやアイコンを変更できます。

記事の題は「**自分の**ユーザー情報を変更する」で、同じ記事の「権限」節は
「**権限の変更は管理者が行えます**」と書いている。**書き分けている**ので、
ハンドルネームは本人が変えられる側だと確定できる。

**→ ユーザーの表示名は第三者（本人）が書ける。** 根B の元の主張が一次情報で裏付いた。

### 到達方法 — **`curl` は 403、`python3 urllib` は 200**

| 経路                                                                          | 結果                                        |
| ----------------------------------------------------------------------------- | ------------------------------------------- |
| `curl`（素 / ブラウザ UA）                                                    | **403**（Cloudflare の `Just a moment...`） |
| `defuddle`                                                                    | **403**                                     |
| `WebFetch`                                                                    | **403**                                     |
| `node` の `fetch`（ブラウザ相当のヘッダ一式）                                 | **403**                                     |
| **`python3` の `urllib.request`**                                             | **200**                                     |
| **Zendesk の Help Center API**（`/api/v2/help_center/ja/articles/<id>.json`） | **200**                                     |

**`DESIGN.md:332` の「この環境から出られない」は偽だった**（2026-09-07 に訂正）。
前セッションも今回も `curl` で試して 403 を見ており、**道具を変えていなかった**。

> **記事本文を JSON で取るほうが確実。** Zendesk の Help Center API は本文を `article.body` に
> 返し、`updated_at` も付く（記事がいつ時点かを確かめられる）。

### 旧: 4経路とも到達できないとした記録（2026-09-07・置き換え済み）

| 経路                                              | 結果                                          |
| ------------------------------------------------- | --------------------------------------------- |
| `support-ja.backlog.com`（素の curl）             | **403**                                       |
| `support-ja.backlog.com`（ブラウザ UA）           | **403**                                       |
| `support-ja.nulab.com`（curl / WebFetch の2通り） | **403**                                       |
| `backlog.com` のブログ記事                        | **404**                                       |
| `developer.nulab.com` の `get-user`（ライブ）     | 応答例のみ。**`name` のフィールド説明が無い** |

> **検索エンジンの要約は根拠に使わない。** 検索は「ヌーラボアカウントの編集でユーザー名を変えると
> Backlog に反映される」という要約を返したが、**その一次ページはどの経路でも読めていない**。
> これは L2 が踏んだのと同じ形（読めない先を根拠に挙げる）なので、**未確認のままにする**。

**結論は変わらない。** コードベース自身の方針（`tools.ts:547`「分からないものは囲む側へ倒す」）で
囲む側に入る。**外部の事実に依存しない根拠だけで足りる。**

### これで偽になる記述（コード中）

| 場所             | 記述                                                                    | 判定                                                                     |
| ---------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `masters.ts:385` | 「**どれも Backlog の管理者が付けた名前**（第三者の自由記述ではない）」 | **偽。** `issueType` / `category` / マイルストーン / 担当者 が該当しない |
| `tools.ts:542`   | 「**選択肢は管理者が定義したもの**なので囲まない（`status` と同型）」   | **偽。** リスト項目の追加は「すべての権限」                              |
| `tools.ts:496`   | 「数値2つなので囲まない」                                               | **真**（`childIssueSummary`。値ではない）                                |

### 症状 → 不変条件

```
症状       ユーザーの表示名が囲まれずに届く（L2-3・C-2）
不変条件   **第三者が書ける値は、すべて囲みを通る**
```

**裏返すと対象が増える。** レンズが挙げたのは1箇所、横断探索で3箇所になり、
**一次情報を引いたら「管理者定義」として素通ししていた4種類が第三者側だった。**

### 対象の全数（`pickName` / `pickNames` の27箇所を分類し直す）

| 囲む（第三者が書ける）                                                     | 箇所 |
| -------------------------------------------------------------------------- | ---- |
| `assignee` / `createdUser` / `updatedUser`                                 | 15   |
| `issueType`                                                                | 1    |
| `category` / `milestone` / `versions`                                      | 3    |
| `tags`（Wiki / Document）                                                  | 3    |
| **囲まない（固定・管理者定義）**                                           |      |
| `status`                                                                   | 2    |
| `priority` / `resolution`                                                  | 2    |
| リスト型カスタム属性の値（`pickNames` 経由・`tools.ts:568`）※ 囲む側へ移動 | 1    |

> **【訂正】** この表は当初リスト型カスタム属性の値を「囲まない」側に置いていたが、**誤り**。
> **リスト項目の追加は「すべての権限」**（更新・削除だけが管理者）なので、第三者が書ける側。

**囲まないのは `status` / `priority` / `resolution` の3種類・4箇所だけ。** 残りはすべて囲む側。

> **副産物**: 実装者コメントと根B が「`pickName` の中で囲む」を確定としていたのは
> **誤りだったが、結論としては正解に近づいた** — 過剰になるのは `status` / `priority` /
> `resolution` の3種類だけになる。それでも**その3つは囲んではいけない**ので、分岐は要る。

### 波及 — **既存テストが動く**（根C・L3-5 と違う）

出力の契約が変わるので、次が落ちる。**これは seam の失敗ではない**（仕様を変える修正なので、
補遺が挙げている例外に当たる）。

| テスト                         | 何を固定しているか                                |
| ------------------------------ | ------------------------------------------------- |
| `tools.test.ts:747`            | `shaped['issueType'] === 'タスク'`                |
| `tools.test.ts:752`            | `shaped['assignee'] === 'eguchi'`                 |
| `tools.test.ts:1751/1753/1754` | `issueTypes` / `categories` / `milestones` の配列 |
| `tools.test.ts:1771/1781/1788` | `assignees` の配列                                |
| `serverE2e.test.ts:668`        | `list_project_masters` の通し                     |

**`status` / `priorities` / `resolutions` のテストは動かないはず** — 過剰に囲んでいないことの記録になる。

### まだ決まっていないこと（段階4 の論点）

**1. `lookupName` のエラーメッセージ（C-2）。** TSDoc が「選べる名前をメッセージに載せる。
LLM が自分で言い直せるようにするため」と書いており、**囲むと言い直しに使えなくなる**。
T-2（添付ファイル名・Wiki ページ名を囲むか）と**同じ構造の対立**が入力側にもある。
`lookupName` は7種類のマスタから呼ばれ、**第三者が書けるのは4種類**なので、
呼び出し元ごとに扱いを変えるか `lookupName` に判断を持たせるかが分かれる。

**2. `list_project_masters` の7フィールド。** `issueTypes` / `categories` / `milestones` /
`assignees` は囲む側、`statuses` / `priorities` / `resolutions` は囲まない側。
**1つの応答の中で扱いが混ざる**ので、読み手が「囲まれていない＝安全」と読めるかを確かめる。

**3. どこで囲むか。** `pickUserName` を作る案は、対象が「ユーザーだけ」でなくなったので
**そのままでは足りない**。`pickName` に「囲むかどうか」を渡す形か、呼び出し側で分けるか。
PII の「唯一の経路」（`tools.test.ts:735` の全走査テスト）を壊さないこと。

**4. コスト。** L3-5 で単価は 107 バイトに下がったが、**囲みの個数はさらに増える**
（1課題あたり `issueType` ＋ `category` ＋ `milestone` ＋ `versions` ＋ 担当者3つ）。
実データでの測り直しは**この環境ではできない**（`.env` を読まない・実 Backlog へ通信しない）。
**構造的な個数はコードから数えられる**ので、そちらで代替するかを決める。

### ユーザーの表示名 — **未確認のまま。ただし材料が1つ増えた**

ミラーに「表示名 / ニックネーム / ハンドルネーム」の語は**0件**、
ユーザーを作る・更新する API も**存在しない**（`/api/v2/users` は GET のみ）。統合者が自分で確認。

**前セッションの論法は弱まる。** 「他のマスタには定義する API があるが表示名には無い」は、
**`priority` / `resolution` にも書き込み API が無い**ので、API の有無だけでは分類の根拠にならない。

**新しい材料**: ミラーの応答例**6件すべて**で `user.name` と `nulabAccount.name` が一致する
（`admin` / `developer` / `eguchi` / `takada` / `testuser2` / `uchida`）。
ユーザーの名前が **Nulab アカウント側に属する**ことを**示唆する**が、
**仕様の記述ではないので確定ではない**。

**結論は変わらない。** コードベース自身の方針（`tools.ts:547`「分からないものは囲む側へ倒す」）に
照らせば、未確認は囲む理由になる。

---

## 段階3（旧）— 未了として書かれていたもの

### 書き出すべきもの（未着手）

**`pickName` の TSDoc が運んでいるもの。**
「ここを通す限り `name` しか出ない ので、経路を増やさないことが要件になる」——
この一文は**フィールド漏洩の要件**を運んでいる。信頼分類を足すときに、
この要件を落とさないこと。`pickName` を分割したり呼び出し側で囲む形にすると、
**「唯一の経路」という性質が崩れる**恐れがある。

**`lookupName` の TSDoc が運んでいるもの。**
「選べる名前をメッセージに載せる。LLM が自分で言い直せるようにするため」——
これは L3 が高く評価した**自己修正できるエラー文言**の根拠。
囲むと識別子として使いにくくなる可能性があり、**T-2（添付ファイル名）と同じ構造の対立**が
ここにもある。`lookupName` は7種類のマスタから呼ばれ、**ユーザーだけが自由記述**。
呼び出し元ごとに扱いを変えるのか、`lookupName` に判断を持たせるのかは未決。

**`list_project_masters` の `members` が運んでいるもの。**
DESIGN.md §9 が記録する2つの事故の産物（1人1件・`loginName` は省略可）。
**囲みを足すときにこの2つの性質を壊さない**こと。

**L1-4 を束ねるかどうか。**
L1-4 は同じ誤分類から出ているが、**修正箇所が違う**（囲みではなく索引の設計）。
束ねると「どの変更がどの指摘に対応したか」が追えなくなる恐れがある。
補遺は「迷ったら分ける側に倒す。ただし同根のものを分けるのは事故」としている。
**同根だが修正が独立、という中間の形。** 判断が要る。

### T-1 との関係

囲む対象を3箇所増やすと、L3-5 が実測した**囲みのコスト（応答の 50.5%）が増える方向**に動く。
`createdUser` はほぼ全応答の全要素に付くので、増分は小さくない。
**根B を当てる前に T-1 の裁定があるほうが、手戻りが少ない。**

---

## 段階4 — 方針（2026-09-07）

### 分ける軸 — **値として現れる名前**か、**名前そのものを列挙する面**か

一次情報で「第三者が書ける」と確定した値は22箇所あるが、**全部を同じに扱えない。**

`list_project_masters` と `lookupName` のエラーメッセージは、**次の呼び出しに渡す名前を
LLM に教えるためにある**。`lookupName` の TSDoc が「選べる名前をメッセージに載せる。
**LLM が自分で言い直せるようにするため**」と書いているとおりで、そこを囲むと
**識別子の面そのものを囲む**ことになる。

**これは T-2（添付ファイル名・Wiki ページ名を囲むか）とまったく同じ構造。**
`tools.ts:1034` が「ファイル名は次の呼び出しに渡す識別子。囲むと識別子として使えない」として
**意図的に囲んでいない**のと同型で、**見落としではなくトレードオフ**である。

### 何を変えるか

**値として現れる名前を囲む。** 対象は `pickName` / `pickNames` の呼び出しのうち次。

| 値                                    | 箇所   |
| ------------------------------------- | ------ |
| `assignee`                            | 2      |
| `createdUser`                         | 8      |
| `updatedUser`                         | 5      |
| `issueType`                           | 1      |
| `category` / `milestone` / `versions` | 3      |
| `tags`（Wiki / Document）             | 3      |
| リスト型カスタム属性の値              | 1      |
| **計**                                | **23** |

**囲まないもの**（一次情報で確定）: `status`（管理者）2箇所、`priority` / `resolution`（口が無い）2箇所。

**囲み方**: 各 shape が既に持っている `wrap(text, field)` を使う薄いヘルパを置く。
`pickName` 自体は変えない — **PII の「唯一の経路」**（`tools.test.ts:735` の全走査テストが固定）を
壊さないため。実装者コメントと段階2 が推した「`pickName` の中で囲む」は、
`status` / `priority` / `resolution` を巻き込むので**採らない**。

### どの指摘が閉じるか

**L2-3 のみ。** そして一次情報で分かった**5種類の誤分類（`issueType` / `category` /
`milestone`・`versions` / タグ / リスト項目）を同時に閉じる** — 同じ根（信頼分類の誤り）で、
片方だけ直すともう片方が同じ形で残る。

**C-2（`lookupName`）は閉じない。T-2 へ移す。**

### 変更しないと決めた箇所（束ねずに外すもの）

| 外すもの                                                   | 理由                                                                              |
| ---------------------------------------------------------- | --------------------------------------------------------------------------------- |
| **`lookupName` のエラーメッセージ**（C-2）                 | **識別子の面**。T-2 と同じ構造なので、**T-2 の裁定に合流させる**                  |
| **`list_project_masters` の7フィールド**                   | 同上。このツールの存在理由が「次の呼び出しに渡す名前を返すこと」                  |
| `ambiguousUserNames` と `note`                             | 同上（指せない名前を案内する面）                                                  |
| ユーザーの表示名を「本人が変更できる」と**文書に書くこと** | 一次情報で確定したので書けるが、**根E（DESIGN.md の乖離）の側**。ここでは書かない |

### 既存テストは**動く**（根C・L3-5 と違う）

出力の契約が変わるので次が落ちる。**これは seam の失敗ではない** — 補遺が挙げる例外
（仕様そのものを変える修正）に当たるので、**方針に明記しておく**。

| テスト                       | 何を固定しているか                 |
| ---------------------------- | ---------------------------------- |
| `tools.test.ts:747`          | `shaped['issueType'] === 'タスク'` |
| `tools.test.ts:752`          | `shaped['assignee'] === 'eguchi'`  |
| `serverE2e.test.ts:254` 付近 | 通しの応答の形                     |

**`status` / `priorities` / `resolutions` と `list_project_masters` のテストは動かないはず** —
**過剰に囲んでいないことの記録**になる。動いたら囲みすぎ。

### コスト

L3-5 で単価は 254 → 107 バイトに下がった。**個数は1課題あたり最大7つ増える**
（`assignee` / `createdUser` / `updatedUser` / `issueType` / `category` / `milestone` / `versions`）。

**実データでの測り直しはこの環境ではできない**（`.env` を読まない・実 Backlog へ通信しない）。
**構造的な個数はコードから数えられる**ので、テストで「1課題あたりの囲みの数」を固定して代替する。

### 順序

1. 赤になるテストを書く（`issueType` / `category` / `tags` / 担当者が囲まれる）
2. **緑のままのはずのテストを書く**（`status` / `priority` / `resolution` は囲まれない・
   `list_project_masters` は変わらない）
3. 各 shape に薄いヘルパを置いて囲む
4. 既存テストの期待値を更新する（**契約を変えたため**。方針に書いたとおり）
5. `masters.ts:385` と `tools.ts:542` の**偽になった TSDoc を直す**
6. `npm run check`

### ここで人間の確認を取る

- **C-2 と `list_project_masters` を T-2 へ移すこと**（根B の範囲が狭まる）
- **既存テストの期待値を書き換えること**（これまでの2件は1件も動かしていない）

---

## L1-4 — 修正済み（2026-09-06）

**根Bから切り出して先に直した。** 誤分類という根は同じだが、**修正箇所が囲みではなく索引**で、
かつ **T-1（囲む対象を広げるか）の裁定を必要としない**ため。

### 症状 → 不変条件

```
症状       表示名 × ログイン名の衝突が検査されていない
不変条件   userIds から引ける名前は、必ず1人を指す
```

**裏返すと衝突の組み合わせが3つ見えた。** 守られていたのは1つ目だけだった。

| 衝突                        | 修正前                              |
| --------------------------- | ----------------------------------- |
| 表示名 × 表示名             | `byName` で数えて `ambiguous` へ ✅ |
| **ログイン名 × 表示名**     | **素通り**（後勝ちで別人に解決）    |
| **ログイン名 × ログイン名** | **素通り**（黙って上書き）          |

### なぜこの2つだけ落ちたのか（型ではなく構造）

**2つの名前空間のうち、片方だけが「一度別の Map に集めてから移す」形になっていた。**
表示名は `byName` という専用の Map を経由するので重複が見える。ログイン名は
「一意だから」という前提で `result` へ直接入れていた。**その前提が検査されていない。**

なお **「ログイン名は一意」の根拠はミラーに無い**（`userId` は応答例に出るだけで、
`add-user` / `update-user` エンドポイント自体が存在しない）。根B の表示名の件と同じ形の未確認前提。
**前提が外れたときに別人へ割り当てるより、引けなくする側へ倒した。**

### 錨

`ambiguousUserNames` ＋ `list_project_masters` の案内が既にある
＝「**指せない名前を明示して案内する**」形が既にあった。**新概念ゼロ。**
「全部のキーを『誰が名乗るか』で数える」1つの規則に置き換えただけで、3通りとも同じ規則で落ちる。

### 波及（全件確認）

`ambiguousUserNames` の消費者は2箇所（`resolveAssignee` / `list_project_masters`）。
`userIds` の消費者は `resolveAssignee` 経由の `lookupName` のみ。

### TDD

赤3件・**境界1件は緑のまま**。

```
actual: 8   ← 表示名×ログイン名。別人に解決されていた
actual: 7   ← 向きを変えても同じ
actual: 8   ← ログイン名同士は黙って上書き
```

**境界テスト**（同じ人のログイン名と表示名が同じなら衝突扱いにしない）は最初から緑。
これが**過剰に直していないことの証拠**になる — DESIGN.md §9 の2回目の事故
（指せるのに候補から消える）と同型を作らないため。

### 違反プローブ

| 違反                                              | 結果                                                     |
| ------------------------------------------------- | -------------------------------------------------------- |
| 衝突検出を外す（後勝ちに戻す）                    | **4件失敗**（新規3件＋既存の「同名は表示名で引けない」） |
| 素朴な修正（同じ名前が2度出たら無条件に衝突扱い） | **境界テストだけが失敗**                                 |

2つ目が要点。**素朴な修正だと境界テストが落ちる**ので、テストが仕事をしている。

### 自己点検 — 自分が偽にした記述を5件直した

`ambiguousUserNames` の意味が「表示名が重複」から「複数の人が名乗る名前」へ広がったので、
寄りかかっていた記述を洗った。**根E（既存の乖離7件）とは別で、今日の修正が偽にしたもの。**

| 場所                           | 何が偽になったか                                                 |
| ------------------------------ | ---------------------------------------------------------------- |
| `masters.ts` の TSDoc 4箇所    | 「表示名は一意ではない」「ログイン名は一意なのでそのまま入れる」 |
| `tools.ts` の案内文            | 「**同名が複数いるため**使えません」                             |
| `DESIGN.md:221`（§5 捨てた案） | 「表示名では引けなくして、**ログイン名で指すよう返す**」         |
| `DESIGN.md:427`（§10）         | 同上                                                             |
| `README.md:288` / `:292`       | 「**同名が2人いたら**表示名では引けなくして…」                   |

**あわせて C-1 の残件も片付けた** — `ApiFailureError` の TSDoc が
「組み立てる側が囲んでから載せる」（人が守る約束）のままだった件。C-1 のときは
「根E と混ざる」として分けたが、**根E は既存の乖離で、これは自分が作った残骸**なので別物だった。
分けた判断のほうが弱かった。

### 結果

`npm run check` 緑。**393件 pass（389 → +4。追加したぶんちょうどで、既存テストは1件も動いていない）。**

### 残り（囲み）は T-1 の裁定待ち

L2-3（`pickName`）・C-2（`lookupName`）・`list_project_masters` の囲みは未着手。
**`pickName` の中で囲む**ことは確定済み（PII 全走査テストが「唯一の経路」を固定している）。

---

## 段階5 — 修正（2026-09-07 完了）

### 結果

`npm run check` 緑。**417件 pass**（408 → 417。増えたのは追加した9件ちょうど）。
**既存テストは4件動いた** — 方針どおり（出力の契約を変えたため）。

| 動いたテスト                                         | 直した内容                                                                                                                                   |
| ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| `名前で表せる項目を返す`                             | 囲むもの（`issueType` / 担当者 / カテゴリー / マイルストーン）と囲まないもの（`status` / `priority` / `resolution`）を**言い分ける形**にした |
| `型ごとに値の読み方を変え、id は1つも出さない`       | リスト型カスタム属性の値が囲まれるようになった                                                                                               |
| `一覧に tags と created を載せる`                    | Wiki のタグが囲まれるようになった                                                                                                            |
| `本文と表題を囲み、id / projectId / json は返さない` | ドキュメントのタグが囲まれるようになった                                                                                                     |

### 当てたもの

**囲んだのは23箇所。** `wrapName` / `wrapNames` をモジュールレベルに置き、各 shape から呼ぶ。
**`pickName` は変えていない** — `status` / `priority` / `resolution` も通るので中で囲むと過剰になり、
ユーザーオブジェクトを載せる唯一の経路という性質も、通す形を変えないことで保つ。

`shapeWikiPage` と `shapeAttachment` に `limits` を足した（囲むのに要る）。呼び出し元は各1箇所。

### 違反プローブ（**当てたことを assert してから打った**）

| 違反                                         | 結果        |
| -------------------------------------------- | ----------- |
| 管理者定義・固定のマスタまで囲む（囲みすぎ） | **3件失敗** |
| 表示名の囲みを1箇所だけ外す                  | **1件失敗** |
| 識別子（添付のファイル名）まで囲む           | **2件失敗** |

L3-5 の段階5 で「プローブの緑は2通りの意味を持つ」と分かったので、今回は**置換が当たったことを
`assert` してから走らせた**。

### 自分が偽にした記述

| 場所                                      | 直した内容                                                                                                         |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `masters.ts` の `lookupName` の TSDoc     | 「どれも管理者が付けた名前」→ **出所は一様ではない**。加えて、ここを囲まない理由（識別子の面・T-2 と同じ）を書いた |
| `tools.ts` の `pickCustomFieldValue` の表 | 「選択肢は管理者が定義したもの」→ **リスト項目の追加は「すべての権限」**                                           |
| `README.md`                               | 囲む／囲まないを、**誰が書けるか**の根拠つきで表にした                                                             |

### 型検査で1件つまずいた（記録）

`assert.deepEqual` を `assert.match` に書き換えたら型検査が落ちた。
`assert/strict` の `deepStrictEqual` は **assertion signature** を持っており、
`shaped.items[0]?.['tags']` の絞り込みが**そこから来ていた**。書き換えると絞り込みが消える。
`const first = shaped.items[0]` を取り出して `assert.ok` する形に直した。

**テストの期待値を書き換えるとき、型の絞り込みが assert から来ていないかを見る。**
